---
phase: 06-audit-&-compliance
plan: 02
type: execute
wave: 3
depends_on: ["06-01", "06-04"]
files_modified:
  - src/app/api/audit/export/request/[requestId]/route.ts
  - src/app/api/audit/export/date-range/route.ts
  - src/lib/export.ts
  - package.json
autonomous: true

user_setup:
  - service: npm
    why: "json2csv library required for CSV generation"
    env_vars: []

must_haves:
  truths:
    - "Admin can export single request audit trail as CSV file"
    - "Admin can export single request audit trail as JSON file"
    - "Admin can export date range audit trail as CSV file"
    - "Admin can export date range audit trail as JSON file"
    - "CSV export includes all fields (timestamp, user, action, comments, status changes)"
    - "JSON export includes full snapshot (request metadata, approvals, files, activities)"
    - "Non-admin requests return 403 Unauthorized"
  artifacts:
    - path: "src/app/api/audit/export/request/[requestId]/route.ts"
      provides: "Single request audit export endpoint"
      exports: ["GET"]
    - path: "src/app/api/audit/export/date-range/route.ts"
      provides: "Date range audit export endpoint"
      exports: ["GET"]
    - path: "src/lib/export.ts"
      provides: "CSV/JSON generation utilities"
      exports: ["generateCSVExport", "generateJSONExport"]
  key_links:
    - from: "src/app/api/audit/export/request/[requestId]/route.ts"
      to: "src/server-actions/audit.ts"
      via: "getAuditTrailForRequest() call"
      pattern: "getAuditTrailForRequest"
    - from: "src/app/api/audit/export/date-range/route.ts"
      to: "src/server-actions/audit.ts"
      via: "getAuditTrailForDateRange() call"
      pattern: "getAuditTrailForDateRange"
    - from: "src/lib/export.ts"
      to: "json2csv library"
      via: "Parser import and parse() call"
      pattern: "new Parser.*parse"
---

<objective>
Build admin-only API endpoints for exporting audit trail data in CSV and JSON formats. Support both single-request exports and date-range exports. Use json2csv for CSV generation with proper escaping. Include full snapshot data (request metadata, approval chain, file attachments) in exports.

Purpose: Enable admins to export audit logs for compliance and external system integration
Output: Route handlers returning CSV/JSON files with proper download headers

NOTE: Export routes call query functions from Plan 06-04 (getAuditTrailForRequest, getAuditTrailForDateRange) instead of implementing inline Prisma queries. This ensures consistent query logic across dashboard and export use cases.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-audit-&-compliance**---immutable-audit-trail-with-export-functionality/06-CONTEXT.md
@.planning/phases/06-audit-&-compliance**---immutable-audit-trail-with-export-functionality/06-RESEARCH.md

@prisma/schema.prisma
@src/lib/auth.ts
@src/server-actions/requests.ts
@src/server-actions/audit.ts
@.planning/phases/06-audit-&-compliance**---immutable-audit-trail-with-export-functionality/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Install json2csv dependency</name>
  <files>package.json</files>
  <action>
Run `npm install json2csv` to add CSV generation library.
This library handles proper CSV escaping (quotes, commas, newlines) per CONTEXT.md requirement.
  </action>
  <verify>Check package.json contains `"json2csv": "^latest"`</verify>
  <done>json2csv installed and available for import</done>
</task>

<task type="auto">
  <name>Create export utility library</name>
  <files>src/lib/export.ts</files>
  <action>
Create src/lib/export.ts with CSV and JSON generation functions.

Per CONTEXT.md locked decisions:
- CSV: One row per action (flat structure)
- JSON: Group by request with nested arrays
- Include both ISO 8601 and user's locale date formats

Export structure should include:
- timestamp (ISO 8601)
- timestampLocale (formatted: "31/01/2025 2:30 PM")
- userId, userName, userEmail
- action
- requestId, requestTitle
- fromStatus, toStatus
- comments
- relatedEntityType, relatedEntityId

For JSON exports, include full snapshot:
```typescript
interface AuditExportJSON {
  requestId: string
  requestTitle: string
  departmentId: string
  departmentName: string
  createdAt: string
  status: string
  requester: { userId, userName, email }
  approvals: Array<{ approvalId, approverName, requiredLevel, status, approvedAt, comments }>
  files: Array<{ fileId, fileName, uploadedBy, uploadedAt }>
  activities: Array<{ activityId, timestamp, userId, userName, action, comments }>
}
```

Use date-fns format() for locale date formatting (already in project).
Use json2csv Parser for CSV generation with fields array defining column order.
  </action>
  <verify>TypeScript compiles without errors, functions export expected types</verify>
  <done>export.ts provides generateCSVExport() and generateJSONExport() with proper typing</done>
</task>

<task type="auto">
  <name>Create single request export endpoint</name>
  <files>src/app/api/audit/export/request/[requestId]/route.ts</files>
  <action>
Create Next.js App Router route handler for single request audit export.

Route: GET /api/audit/export/request/[requestId]?format=csv|json

Implementation:
1. Await params to get requestId (Next.js 15 async params pattern)
2. Call requireAdmin() to verify admin access - return 403 if not admin
3. Call getAuditTrailForRequest(requestId) from src/server-actions/audit.ts (Plan 06-04) to get activities with user/request includes
4. Also fetch Request with approvals and fileAttachments for full snapshot (separate Prisma query for snapshot data)
5. Call generateCSVExport() or generateJSONExport() based on format query param
6. Return Response with appropriate headers:
   - CSV: 'Content-Type: text/csv; charset=utf-8'
   - JSON: 'Content-Type: application/json; charset=utf-8'
   - 'Content-Disposition: attachment; filename="audit-trail-{requestId}.{csv|json}"'
   - 'Cache-Control: no-cache, no-store, must-revalidate'

Default format: CSV if not specified

Error handling:
- 403 if not admin
- 404 if request not found
- 500 on server error

Per CONTEXT.md: Include all data (no PII masking), full transparency for admin.
  </action>
  <verify>Call endpoint as admin: curl returns CSV/JSON with proper headers
Call endpoint as non-admin: returns 403
Test with request with existing activities</verify>
  <done>Admin can export single request audit trail in CSV or JSON format</done>
</task>

<task type="auto">
  <name>Create date range export endpoint</name>
  <files>src/app/api/audit/export/date-range/route.ts</files>
  <action>
Create Next.js App Router route handler for date range audit export.

Route: GET /api/audit/export/date-range?startDate=YYYY-MM-DD&endDate=YYYY-MM-DD&format=csv|json

Implementation:
1. Parse startDate and endDate from query params
2. Validate date range (max 90 days per RESEARCH.md Pitfall #2 to prevent timeout)
3. Call requireAdmin() to verify admin access - return 403 if not admin
4. Set endDate to end of day (23:59:59.999) for inclusive filtering
5. Call getAuditTrailForDateRange(startDate, endDate) from src/server-actions/audit.ts (Plan 06-04) - reuses consistent query logic with pagination
6. Include related request data for context (already included by query API)
7. Call generateCSVExport() or generateJSONExport()
8. Return Response with same headers as single request export
9. Filename: "audit-trail-{startDate}-to-{endDate}.{csv|json}"

Validation requirements from RESEARCH.md:
- If date range > 90 days, return 400 error with message
- Parse dates with Date() constructor, validate not NaN

Per CONTEXT.md: Export includes full snapshot data even for date ranges.
  </action>
  <verify>Call endpoint with valid date range as admin: returns file
Call with >90 day range: returns 400 error
Call as non-admin: returns 403
Test date inclusiveness (endDate should include that day's records)</verify>
  <done>Admin can export date range audit trail with 90-day limit validation</done>
</task>

</tasks>

<verification>
1. json2csv installed in package.json
2. export.ts generates valid CSV with proper escaping
3. export.ts generates valid JSON with full snapshot structure
4. Single request endpoint returns CSV/JSON with download headers
5. Date range endpoint validates 90-day limit
6. Non-admin requests return 403
7. All exported data includes no PII masking (full transparency)
</verification>

<success_criteria>
- Admins can export audit trail for individual requests in CSV or JSON format
- Admins can export audit trail for date ranges (max 90 days) in CSV or JSON format
- CSV exports use one-row-per-action format with all fields
- JSON exports use nested structure with full request snapshot
- Non-admin users cannot access export endpoints
- Exported files trigger browser download (not inline display)
</success_criteria>

<output>
After completion, create `.planning/phases/06-audit-&-compliance**---immutable-audit-trail-with-export-functionality/06-02-SUMMARY.md`
</output>
