---
phase: 08-complete-admin-user-and-department-management
plan: 03
type: execute
wave: 1
depends_on: [08-01]
files_modified: [prisma/schema.prisma, src/server-actions/users.ts, src/lib/export.ts]
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Admin can update user details without foreign key violation errors"
    - "System-level activities (like user updates) are logged without a request ID"
    - "Audit trail exports handle system activities correctly"
  artifacts:
    - path: "prisma/schema.prisma"
      contains: "requestId String?"
    - path: "src/server-actions/users.ts"
      contains: "requestId: null"
    - path: "src/lib/export.ts"
      contains: "request: {"
  key_links:
    - from: "src/server-actions/users.ts"
      to: "prisma.requestActivity"
      via: "create with null requestId"
---

<objective>
Fix foreign key violation in user update workflow by allowing system-level audit logs in RequestActivity table.

Purpose: Enable admins to update user details while maintaining a complete audit trail of changes, resolving the blocker issue where 'SYSTEM' requestId violated database constraints.
Output: Schema update making requestId optional, updated server actions, and resilient export logic.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-complete-admin-user-and-department-management/08-UAT.md
@prisma/schema.prisma
@src/server-actions/users.ts
@src/lib/export.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema for System Activities</name>
  <files>prisma/schema.prisma</files>
  <action>
    Modify `RequestActivity` model in `prisma/schema.prisma`:
    1. Change `requestId` from `String` to `String?` (optional).
    2. Change `request` relation to `Request?` (optional).
    
    Then run migration:
    `npx prisma migrate dev --name make_request_activity_request_id_optional`
  </action>
  <verify>
    Check schema file for `requestId String?` and run `npx prisma validate` to ensure validity.
  </verify>
  <done>
    Schema updated and migration created/applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Export Logic for Null Requests</name>
  <files>src/lib/export.ts</files>
  <action>
    Update `src/lib/export.ts` to handle nullable request relations in audit records:
    
    1. Update `AuditActivityRecord` type definition:
       - Make `request` property matching the type in `src/server-actions/audit.ts` (it might be nullable if requestId is null, though Prisma include usually returns null for the relation property).
       - Actually, if `requestId` is null, `request` will be null.
       - Type should be: `request: { ... } | null`
    
    2. Update `generateCSVExport` function:
       - Add safe access (optional chaining `?.`) for all request properties.
       - Handle null values gracefully (e.g., empty string or "System").
       - `requestId: activity.request?.id || 'SYSTEM'`
       - `requestTitle: activity.request?.title || 'System Activity'`
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify type safety.
  </verify>
  <done>
    Export logic handles system activities without crashing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix User Update Action</name>
  <files>src/server-actions/users.ts</files>
  <action>
    Update `updateUser` function in `src/server-actions/users.ts`:
    
    1. Locate the `prisma.requestActivity.create` call inside the `changes.length > 0` block.
    2. Remove `requestId: 'SYSTEM'`.
    3. Since `requestId` is now optional (and defaults to null if omitted), this allows creating the record without a foreign key violation.
    
    Note: Do not pass `requestId: null` explicitly if the type expects a string or undefined; usually omitting it is safer with Prisma's optional fields, or pass `undefined`.
  </action>
  <verify>
    Simulate the fix by checking the code. Since we can't easily run the app in this environment, reliance on type check and code correctness is key.
  </verify>
  <done>
    updateUser logs activities without using invalid foreign keys.
  </done>
</task>

</tasks>

<verification>
Manual verification required after deployment:
1. Login as Admin.
2. Go to Users management.
3. Edit a user (change name or role).
4. Save.
5. Verify success message (no error).
6. Verify audit trail export includes this activity.
</verification>

<success_criteria>
- [ ] requestId is optional in RequestActivity schema
- [ ] updateUser action succeeds when logging changes
- [ ] Audit exports allow records without request data
</success_criteria>

<output>
After completion, create `.planning/phases/08-complete-admin-user-and-department-management/08-03-SUMMARY.md`
</output>
