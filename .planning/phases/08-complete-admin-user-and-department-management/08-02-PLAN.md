---
phase: 08
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server-actions/departments.ts
  - src/components/admin/department-form.tsx
autonomous: true
must_haves:
  truths:
    - Admin can rename a department
    - Admin can change a department's type
    - Department names must be unique (case-insensitive check recommended)
    - Updates persist to database
    - Department changes (name/type) do not affect approval workflow (uses department ID)
  artifacts:
    - path: src/server-actions/departments.ts
      provides: updateDepartment with validation
    - path: src/components/admin/department-form.tsx
      provides: Form with editable type selection
  key_links:
    - from: src/components/admin/department-form.tsx
      to: src/server-actions/departments.ts
      via: updateDepartment action
---

<objective>
Finalize Department Edit functionality by adding robust validation, ensuring name uniqueness, and enabling Type editing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/server-actions/departments.ts
@src/components/admin/department-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Type Editing in UI</name>
  <files>src/components/admin/department-form.tsx</files>
  <action>
    Modify `DepartmentForm` to include the "Type" field if missing or make it editable.
    - Add a `<Select>` input for `type` (options should match the `DepartmentType` enum: e.g., BILLABLE, INTERNAL, etc.).
    - Ensure the `form.control` is properly typed and connected to the `type` field.
    - Update the validation schema (zod) to include `type`.
  </action>
  <verify>
    `DepartmentForm` contains a Select component for the `type` field.
  </verify>
  <done>
    Admin can select and change the department type in the form.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce Name Uniqueness and Handle Type Update</name>
  <files>src/server-actions/departments.ts</files>
  <action>
    Modify `updateDepartment` in `src/server-actions/departments.ts`:
    
    1. **Name Uniqueness**:
       - Check if `input.name` is being changed.
       - If changed, query Prisma to ensure no *other* department (excluding `input.id`) has the same name.
       - Throw "Department name already exists" if a conflict is found.
    
    2. **Type Update**:
       - Ensure `input.type` is passed to the Prisma update call.
    
    3. **Create Validation**:
       - Also add a similar name uniqueness check to `createDepartment` (currently only checks ID).
  </action>
  <verify>
    `updateDepartment` contains logic to update `type` and a `prisma.department.findFirst` check for name conflict.
  </verify>
  <done>
    Department updates persist name and type changes, with uniqueness validation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Department Edit Functionality</what-built>
  <how-to-verify>
    1. Go to Admin > Departments.
    2. Edit a department (e.g., "Quality Control").
    3. Change name to "QC Updated" and change Type (e.g., from INTERNAL to BILLABLE). Save.
    4. Verify table updates and reflects the new Type.
    5. Try to rename it to an existing name (e.g., "Engineering"). Verify error message.
  </how-to-verify>
  <resume-signal>Type "verified" if department editing (name & type) works correctly</resume-signal>
</task>

</tasks>

<verification>
Unit verification via checkpoint.
</verification>

<success_criteria>
- Department updates are validated for uniqueness.
- Admin can modify Department Type.
- UI correctly displays errors from server action.
</success_criteria>

<output>
After completion, create .planning/phases/08-admin/08-02-SUMMARY.md
</output>
