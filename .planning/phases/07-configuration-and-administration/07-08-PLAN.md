---
phase: 07-configuration-and-administration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/user-table.tsx
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Deactivating a user triggers a confirmation dialog"
    - "Clicking Cancel in the dialog aborts deactivation"
    - "Clicking Confirm in the dialog executes deactivation and refreshes the list"
    - "Deactivation does not fire immediately on menu click"
  artifacts:
    - path: src/components/admin/user-table.tsx
      contains: "AlertDialog"
  key_links:
    - from: "DropdownMenuItem (Deactivate)"
      to: "state: userToDeactivate"
      via: "onClick (no direct server call)"
---

<objective>
Refactor UserTable to ensure robust deactivation confirmation, replacing the immediate-fire or fragile implementation with a deterministic state-driven dialog flow.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/admin/user-table.tsx
@src/server-actions/users.ts
</context>

<tasks>

<task type="auto">
  <name>Refactor UserTable deactivation flow</name>
  <files>src/components/admin/user-table.tsx</files>
  <action>
    1. Import `useRouter` from `next/navigation` and replace `window.location.reload()` with `router.refresh()`.
    2. Modify the "Deactivate" `DropdownMenuItem`:
       - Add `onSelect={(e) => e.preventDefault()}` to prevent menu interaction issues (optional but good practice).
       - Ensure `onClick` ONLY sets `userToDeactivate` state.
    3. Verify `handleToggleActive` is NOT called directly for deactivation.
    4. Ensure `AlertDialog` uses `userToDeactivate` state correctly.
    5. Add a loading state (`isPending`) to the `AlertDialogAction` to prevent double-submission.
  </action>
  <verify>
    Scan file to confirm `handleToggleActive` is only called inside `AlertDialogAction` (for deactivation) and `DropdownMenuItem` (for activation).
  </verify>
  <done>
    User deactivation flow requires explicit dialog confirmation and handles loading states gracefully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Refactored user deactivation flow</what-built>
  <how-to-verify>
    1. Go to Admin > Users.
    2. Click actions (...) on an Active user.
    3. Click "Deactivate".
    4. Verify: Dropdown closes, Confirmation Dialog opens.
    5. Click "Cancel". Verify: User remains Active.
    6. Click "Deactivate" again, then Confirm.
    7. Verify: Dialog closes, table refreshes (without full page reload), user becomes Inactive.
  </how-to-verify>
  <resume-signal>Type "verified" if the flow works as expected.</resume-signal>
</task>

</tasks>

<success_criteria>
- [ ] Deactivation requires confirmation dialog
- [ ] No immediate server calls on "Deactivate" menu click
- [ ] UI refreshes using Next.js router instead of window reload
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-08-SUMMARY.md`
</output>
