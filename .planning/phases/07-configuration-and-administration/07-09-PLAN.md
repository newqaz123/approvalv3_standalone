---
phase: 07-configuration-and-administration
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/hierarchy-view.tsx
  - src/server-actions/hierarchy.ts
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Hierarchy changes are not saved immediately on drop"
    - "User sees a 'Save Changes' option after modifying hierarchy"
    - "Saving validates the entire hierarchy structure"
    - "Invalid configurations (e.g., empty levels) are rejected with an error"
  artifacts:
    - path: src/components/admin/hierarchy-view.tsx
      contains: "pendingChanges"
    - path: src/server-actions/hierarchy.ts
      contains: "validateHierarchyUpdates"
  key_links:
    - from: "HierarchyView Save Button"
      to: "updateHierarchy"
      via: "server action with full diff"
---

<objective>
Implement a "Batch Save" workflow for the Hierarchy Builder to provide a preview-before-save experience and ensure robust validation of hierarchy rules (minimum one level, no empty levels).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/admin/hierarchy-view.tsx
@src/server-actions/hierarchy.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Batch Save UI in HierarchyView</name>
  <files>src/components/admin/hierarchy-view.tsx</files>
  <action>
    Refactor `HierarchyView` to support batch mode:
    1. Remove `pendingMove` / `AlertDialog` logic that triggers on every drop.
    2. Add `hasChanges` state (derived from comparing `usersByLevel` vs `initialUsersByLevel`).
    3. Update `handleDragEnd` to only update local `usersByLevel` state (optimistic UI only).
    4. Add a "Save Changes" footer/bar that appears when `hasChanges` is true.
       - Include "Save" button.
       - Include "Reset" button (reverts to `initialUsersByLevel`).
    5. Implement `handleSave` function:
       - Calculates diffs (users who changed levels).
       - Calls `updateHierarchy` with all updates.
       - Handles success (toast, refresh) and error (toast, no refresh).
  </action>
  <verify>
    Check that `handleDragEnd` no longer calls server actions or opens dialogs directly.
    Check that `handleSave` constructs the correct payload for `updateHierarchy`.
  </verify>
  <done>
    Hierarchy builder allows multiple drag-and-drop operations without persistence until "Save" is clicked.
  </done>
</task>

<task type="auto">
  <name>Verify and Reinforce Hierarchy Validation</name>
  <files>src/server-actions/hierarchy.ts</files>
  <action>
    Review `validateHierarchyUpdates` in `src/server-actions/hierarchy.ts`:
    1. Ensure `finalLevels.length === 0` check is robust (it appears correct, but ensure it handles edge cases).
    2. Ensure "no empty levels" check is robust.
    3. Add a specific check: If the department has users, but `finalLevels` implies all are removed (if that were possible), reject.
    4. (Optional) Add unit-test-like comments or a small verification script if needed, but the code review is primary.
  </action>
  <verify>
    Code review of validation logic ensures it rejects invalid states.
  </verify>
  <done>
    Server-side validation strictly enforces hierarchy rules.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hierarchy Batch Save & Validation</what-built>
  <how-to-verify>
    1. Go to Admin > Hierarchy.
    2. Drag a user from Level 1 to Level 2.
    3. Verify: No immediate save/dialog. "Unsaved Changes" bar appears.
    4. Drag another user.
    5. Click "Reset". Verify: Users return to original positions, bar disappears.
    6. Drag a user again. Click "Save".
    7. Verify: Success toast, data persists.
    8. (Edge Case) Try to create an invalid state (e.g., empty Level 1 by moving everyone to Level 2).
    9. Click "Save". Verify: Error message "Level 1 has no approvers" (or similar).
  </how-to-verify>
  <resume-signal>Type "verified" if the batch save and validation work.</resume-signal>
</task>

</tasks>

<success_criteria>
- [ ] Hierarchy changes require explicit "Save" action
- [ ] Users can reset pending changes
- [ ] Validation prevents saving invalid hierarchies (e.g. empty levels)
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-09-SUMMARY.md`
</output>
