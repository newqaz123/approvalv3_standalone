---
phase: 07-configuration-and-administration
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/admin/hierarchy-view.tsx
  - src/components/admin/hierarchy-column.tsx
  - src/components/admin/hierarchy-user-card.tsx
  - src/app/admin/hierarchy/page.tsx
  - src/app/dashboard/workflow/page.tsx
autonomous: true
must_haves:
  truths:
    - "Hierarchy visualizes custom level names"
    - "Users can be dragged between approval levels (Admin only)"
    - "Hierarchy management page exists"
    - "All users can view read-only hierarchy"
  artifacts:
    - path: "src/components/admin/hierarchy-view.tsx"
      contains: "readOnly"
    - path: "src/app/admin/hierarchy/page.tsx"
      contains: "HierarchyView"
    - path: "src/app/dashboard/workflow/page.tsx"
      contains: "readOnly={true}"
  key_links:
    - from: "page.tsx"
      to: "HierarchyView"
      via: "import"
---

<objective>
Implement the Hierarchy Management Page and a public Read-Only Workflow view.

Purpose: Provide an intuitive interface for admins to manage approvals and for users to understand the workflow.
Output: Fully functional Hierarchy Management page and public read-only view.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/admin/hierarchy-view.tsx
@src/components/admin/hierarchy-column.tsx
@src/components/admin/hierarchy-user-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Hierarchy Components</name>
  <files>src/components/admin/hierarchy-view.tsx, src/components/admin/hierarchy-column.tsx</files>
  <action>
    1. Update `hierarchy-view.tsx`:
       - Accept `levelNames` (JSON/Record) in the department prop.
       - Add `readOnly` boolean prop (default false).
       - If `readOnly` is true, disable drag-and-drop (e.g., wrap `DndContext` in conditional or disable sensors).
       - Pass the custom name (e.g., "Supervisor") to `HierarchyColumn`.
    2. Update `hierarchy-column.tsx`:
       - Accept and display the custom label for the level header.
       - Ensure it falls back to "Level N" if no custom name exists.
  </action>
  <verify>
    Create a temporary test rendering of `HierarchyView` with `readOnly={true}`.
    Verify columns show names but items cannot be dragged.
  </verify>
  <done>
    Hierarchy components support custom level names and read-only mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Admin Hierarchy Page (Editable)</name>
  <files>src/app/admin/hierarchy/page.tsx</files>
  <action>
    1. Create `src/app/admin/hierarchy/page.tsx`:
       - Scaffold the page layout (AdminShell/Layout).
       - Add a Department selector.
       - Fetch department data (including `levelNames`) and users.
       - Render `HierarchyView` with `readOnly={false}`.
       - Handle state for selected department.
  </action>
  <verify>
    Visit `/admin/hierarchy`.
    Select a department.
    Verify the hierarchy board loads and supports drag-and-drop.
  </verify>
  <done>
    Interactive drag-and-drop hierarchy page is fully functional for admins.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Read-Only Workflow View</name>
  <files>src/app/dashboard/workflow/page.tsx</files>
  <action>
    1. Create `src/app/dashboard/workflow/page.tsx`:
       - Use the standard dashboard layout (Sidebar/Shell).
       - Fetch the user's department data (or allow viewing all if permitted).
       - Render `HierarchyView` with `readOnly={true}`.
       - Add a header explaining "This is the approval workflow for your department".
  </action>
  <verify>
    Log in as a standard user.
    Visit `/dashboard/workflow`.
    Verify the hierarchy is visible but users cannot be dragged.
  </verify>
  <done>
    Read-only view of the approval hierarchy is accessible to all users.
  </done>
</task>

</tasks>

<verification>
- Admin view supports DND.
- Public view is read-only.
- Custom level names appear in both views.
</verification>

<success_criteria>
- [ ] Hierarchy Page created for admins (editable)
- [ ] Workflow Page created for users (read-only)
- [ ] Existing DND components reused with `readOnly` prop
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-03-SUMMARY.md`
</output>
