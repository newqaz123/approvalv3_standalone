---
phase: 07-configuration-and-administration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
autonomous: true
must_haves:
  truths:
    - "Schema supports naming approval levels per department"
    - "Schema supports cross-department approvers via DepartmentApprover"
    - "Schema supports request archival status"
    - "Database seeded with realistic hierarchy data"
  artifacts:
    - path: "prisma/schema.prisma"
      contains: "model DepartmentApprover"
    - path: "prisma/seed.ts"
      contains: "levelNames"
  key_links:
    - from: "DepartmentApprover"
      to: "User"
      via: "approverId"
---

<objective>
Update the data model to support configurable approval hierarchies, cross-department approvers, and request archival.

Purpose: Provide the database foundation for the drag-and-drop hierarchy builder and admin configuration tools.
Output: Updated Prisma schema, migration, and seeded database with hierarchy data.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@prisma/schema.prisma
@prisma/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Schema for Hierarchy Configuration</name>
  <files>prisma/schema.prisma</files>
  <action>
    Modify `prisma/schema.prisma`:
    1. Add `levelNames Json?` to `Department` model (to store map of Level Int -> "Supervisor", "Manager", etc).
    2. Add `isArchived Boolean @default(false)` to `Request` model.
    3. Create `DepartmentApprover` model:
       - `id String @id @default(cuid())`
       - `departmentId String` (Relation to Department)
       - `approverId String` (Relation to User)
       - `approverLevel Int` (The level this user approves for in THIS department)
       - `createdAt`, `updatedAt`
       - Unique constraint on `[departmentId, approverId, approverLevel]`
       - Add relations to `Department` and `User`.
    4. Run `npx prisma migrate dev --name add_hierarchy_config`.
  </action>
  <verify>
    Run `npx prisma format` to ensure valid schema.
    Check migration file creation in `prisma/migrations`.
  </verify>
  <done>
    Schema updated and migration created successfully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Seed Script with Hierarchy Data</name>
  <files>prisma/seed.ts</files>
  <action>
    Update `prisma/seed.ts`:
    1. Update `Department` creation to include `levelNames` (e.g., `{1: "Supervisor", 2: "Manager", 3: "Director"}`).
    2. Assign `level` (1, 2, 3) to existing users in `upsert` calls.
    3. Create at least one `DepartmentApprover` example (e.g., an Engineering Manager being an approver for Maintenance Level 3).
    4. Run `npx prisma db seed`.
  </action>
  <verify>
    Run `npx prisma db seed` and check for "Seeding finished" message.
    Inspect database to verify `levelNames` are populated.
  </verify>
  <done>
    Seed script runs without errors and populates new fields.
  </done>
</task>

</tasks>

<verification>
Run `npx prisma studio` (optional) or check database directly to confirm:
- Departments have `levelNames` JSON.
- Users have `level` Int set.
- `DepartmentApprover` table exists and has data.
</verification>

<success_criteria>
- [ ] Schema migration applied successfully
- [ ] Seed data includes hierarchy configuration
- [ ] `DepartmentApprover` model available for Phase 7 logic
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-01-SUMMARY.md`
</output>
