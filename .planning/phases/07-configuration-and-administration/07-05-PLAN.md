---
phase: 07-configuration-and-administration
plan: 05
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/api/cron/archive/route.ts
  - vercel.json
  - src/server-actions/requests.ts
  - src/app/admin/retention/page.tsx
  - src/components/admin/retention-controls.tsx
autonomous: false
user_setup:
  - service: vercel_cron
    why: "Automated archival of old requests"
    dashboard_config:
      - task: "Verify Cron Job appears in dashboard"
        location: "Vercel Dashboard -> Settings -> Cron Jobs"
must_haves:
  truths:
    - "Old completed requests are marked as archived"
    - "Archived requests are hidden from default lists"
    - "Archived requests can be viewed via filter"
    - "Admins can manually archive/delete requests"
  artifacts:
    - path: "src/app/api/cron/archive/route.ts"
      contains: "GET"
    - path: "vercel.json"
      contains: "crons"
    - path: "src/components/admin/retention-controls.tsx"
      contains: "archiveRequest"
  key_links:
    - from: "vercel.json"
      to: "/api/cron/archive"
      via: "cron schedule"
---

<objective>
Implement automated request archival (Vercel Cron) and manual retention controls.

Purpose: Keep the active database queries fast and provide admins with tools to manage data lifecycle.
Output: API endpoint for cron, cron config, and Admin UI for manual archival/deletion.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@vercel.json
@src/app/api/cron/archive/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Archival API Endpoint</name>
  <files>src/app/api/cron/archive/route.ts</files>
  <action>
    Create a GET endpoint at `/api/cron/archive`:
    1. Verify authorization (check `Authorization` header matches `CRON_SECRET` or Vercel signature).
    2. Logic: Find requests where `status` is `Completed` or `Cancelled` AND `updatedAt` > 90 days ago (configurable).
    3. Update them: Set `isArchived = true`.
    4. Return JSON summary (count archived).
  </action>
  <verify>
    Run `curl` against local endpoint (mocking auth).
    Verify old requests are updated in DB.
  </verify>
  <done>
    Secure API endpoint for archival exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Cron and Filters</name>
  <files>vercel.json, src/server-actions/requests.ts</files>
  <action>
    1. Update `vercel.json` to add a `crons` entry pointing to `/api/cron/archive` (schedule: daily, e.g., "0 0 * * *").
    2. Update `src/server-actions/requests.ts` (`getRequests` etc.):
       - By default, add `where: { isArchived: false }`.
       - Allow passing `includeArchived: true` to override.
  </action>
  <verify>
    Check `vercel.json` syntax.
    Verify dashboard lists EXCLUDE archived requests by default.
  </verify>
  <done>
    Cron scheduled and app logic respects archival status.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Manual Retention Controls</name>
  <files>src/app/admin/retention/page.tsx, src/components/admin/retention-controls.tsx, src/server-actions/requests.ts</files>
  <action>
    1. Update `src/server-actions/requests.ts`:
       - Add `archiveRequest(requestId: string)`: Sets `isArchived = true`.
       - Add `deleteRequest(requestId: string)`: Performs `prisma.request.delete`.
       - Ensure both check for Admin role.
    2. Create `src/components/admin/retention-controls.tsx`:
       - A component that takes a Request ID or list of IDs.
       - Buttons for "Archive" and "Delete Permanently".
       - Confirm dialogs for deletion.
    3. Create `src/app/admin/retention/page.tsx`:
       - Simple Admin table listing ALL requests (including archived).
       - Columns: ID, Status, Date, Is Archived.
       - Filters: Show Active / Show Archived.
       - Actions column: Uses `RetentionControls`.
  </action>
  <verify>
    Visit `/admin/retention`.
    Archive an active request -> Status updates.
    Delete a request -> Disappears from list.
  </verify>
  <done>
    Admins can manually manage request lifecycle via UI.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Vercel Cron Job Configuration & Manual Controls</what-built>
  <how-to-verify>
    1. Push changes to Vercel.
    2. Go to Vercel Dashboard -> Project -> Settings -> Cron Jobs.
    3. Verify the "archive-requests" job appears in the list.
    4. Visit `/admin/retention` and test manual archive/delete.
  </how-to-verify>
  <resume-signal>Type "verified" if the cron job appears and manual controls work.</resume-signal>
</task>

</tasks>

<verification>
- Cron job endpoint returns 200 and archives requests.
- Dashboard does not show archived requests.
- Admin Retention page shows ALL requests and allows actions.
</verification>

<success_criteria>
- [ ] Archival API functional and secure
- [ ] Cron job configured in `vercel.json`
- [ ] Manual Retention UI available to Admins
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-05-SUMMARY.md`
</output>
