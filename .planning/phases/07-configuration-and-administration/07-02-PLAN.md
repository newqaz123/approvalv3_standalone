---
phase: 07-configuration-and-administration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/admin/departments/page.tsx
  - src/app/admin/users/page.tsx
  - src/components/admin/department-form.tsx
  - src/components/admin/edit-department-dialog.tsx
  - src/components/admin/user-form.tsx
  - src/components/admin/edit-user-dialog.tsx
  - src/server-actions/departments.ts
  - src/server-actions/users.ts
autonomous: true
must_haves:
  truths:
    - "Admins can edit department level names"
    - "Admins can assign approval levels to users"
    - "Department list shows configured levels"
    - "User list shows assigned level"
  artifacts:
    - path: "src/components/admin/department-form.tsx"
      contains: "levelNames"
    - path: "src/components/admin/user-form.tsx"
      contains: "level"
    - path: "src/components/admin/edit-department-dialog.tsx"
      contains: "DepartmentForm"
  key_links:
    - from: "EditDepartmentDialog"
      to: "DepartmentForm"
      via: "props"
    - from: "DepartmentForm"
      to: "updateDepartment"
      via: "server action"
---

<objective>
Update Admin Management UI to support the new hierarchy configuration fields.

Purpose: Allow admins to configure department level names (e.g., "Level 1 = Supervisor") and manually assign user levels.
Output: Enhanced Department and User management forms with Edit capabilities.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/admin/departments/page.tsx
@src/app/admin/users/page.tsx
@src/components/admin/department-form.tsx
@src/components/admin/create-department-dialog.tsx
@src/components/admin/user-form.tsx
@src/server-actions/departments.ts
@src/server-actions/users.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Department Form & Create Edit Dialog</name>
  <files>src/components/admin/department-form.tsx, src/components/admin/edit-department-dialog.tsx, src/app/admin/departments/page.tsx, src/server-actions/departments.ts</files>
  <action>
    1. Update `department-form.tsx`:
       - Add a section to configure "Level Names".
       - Simple UI: List of inputs (Level 1, Level 2, Level 3, etc.) mapped to a JSON object.
       - Allow adding/removing levels (up to ~5 levels).
    2. Create `src/components/admin/edit-department-dialog.tsx`:
       - Wrap `DepartmentForm` and pass `initialData`.
       - Add a trigger button (Edit icon).
    3. Update `src/app/admin/departments/page.tsx`:
       - Integrate `EditDepartmentDialog` into the departments table.
    4. Update `src/server-actions/departments.ts`:
       - Update schemas to accept `levelNames`.
       - Ensure persistence to Prisma.
  </action>
  <verify>
    Navigate to Admin -> Departments.
    Click "Edit" on a department.
    Add "Level 1: Supervisor", Save.
    Refresh and verify persistence.
  </verify>
  <done>
    Department level names can be configured via UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance User Form & Create Edit Dialog</name>
  <files>src/components/admin/user-form.tsx, src/components/admin/edit-user-dialog.tsx, src/app/admin/users/page.tsx, src/server-actions/users.ts</files>
  <action>
    1. Update `user-form.tsx`:
       - Add a `Select` or `Input` for `level` (Int).
       - Show this field only if a Department is selected.
    2. Create `src/components/admin/edit-user-dialog.tsx`:
       - Wrap `UserForm` and pass `initialData`.
       - Add a trigger button (Edit icon).
    3. Update `src/app/admin/users/page.tsx`:
       - Integrate `EditUserDialog` into the users table.
    4. Update `src/server-actions/users.ts`:
       - Update `updateUser` schema to accept `level`.
       - Persist `level` to Prisma.
  </action>
  <verify>
    Navigate to Admin -> Users.
    Click "Edit" on a user.
    Assign Level 2, Save.
    Verify in database or UI list.
  </verify>
  <done>
    User approval levels can be assigned via UI.
  </done>
</task>

</tasks>

<verification>
- Check that `Department.levelNames` is correctly stored as JSON.
- Check that `User.level` is correctly stored as Int.
- UI validation prevents invalid inputs (e.g., negative levels).
</verification>

<success_criteria>
- [ ] Admins can name approval levels for each department
- [ ] Admins can assign users to specific approval levels
- [ ] Edit dialogs are functional and integrated into tables
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-02-SUMMARY.md`
</output>
