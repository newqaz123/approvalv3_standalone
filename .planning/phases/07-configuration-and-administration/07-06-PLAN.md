---
phase: 07-configuration-and-administration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/admin/user-table.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Deactivating a user shows a confirmation dialog before executing"
    - "Confirmation dialog clearly states the action (deactivate) and the user's name"
    - "Cancelling the confirmation does not deactivate the user"
  artifacts:
    - path: "src/components/admin/user-table.tsx"
      provides: "AlertDialog wrapping deactivate action"
      contains: "AlertDialog"
  key_links:
    - from: "user-table.tsx AlertDialog confirm button"
      to: "deactivateUser server action"
      via: "onClick handler inside AlertDialogAction"
      pattern: "deactivateUser"
---

<objective>
Add confirmation dialog to user deactivation action.

Purpose: Gap closure — VERIFICATION.md found that the Deactivate action in user-table.tsx fires immediately without a confirmation prompt. The success criteria requires "confirmation prompts" for user deactivation.

Output: Updated user-table.tsx with AlertDialog wrapping the Deactivate dropdown menu action.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/admin/user-table.tsx
@src/components/admin/retention-controls.tsx (reference for AlertDialog pattern already used in this codebase)
@src/components/ui/alert-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AlertDialog confirmation to user deactivation</name>
  <files>src/components/admin/user-table.tsx</files>
  <action>
    Wrap the Deactivate action in user-table.tsx with an AlertDialog confirmation prompt. Follow the pattern already established in `retention-controls.tsx` which uses AlertDialog from `@/components/ui/alert-dialog`.

    Specific changes to `src/components/admin/user-table.tsx`:

    1. Import AlertDialog components: `AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger` from `@/components/ui/alert-dialog`.

    2. In the actions column cell (around line 110-143), replace the current `DropdownMenuItem` that directly calls `handleToggleActive` for the deactivation case. Only the "Deactivate" action needs confirmation — "Activate" (reactivating an inactive user) can remain direct since it's non-destructive.

    3. Implementation approach: Use `AlertDialog` with `AlertDialogTrigger` wrapping a styled element inside the dropdown. Since DropdownMenu and AlertDialog can conflict, use a state-driven approach:
       - Add a `useState<string | null>` for `userToDeactivate` (stores user ID pending confirmation).
       - When the "Deactivate" DropdownMenuItem is clicked, set `userToDeactivate` to the user's ID and user name instead of calling handleToggleActive directly.
       - Render an AlertDialog (outside the table, controlled by `open={!!userToDeactivate}`) that shows:
         - Title: "Deactivate User"
         - Description: "Are you sure you want to deactivate {userName}? They will no longer be able to log in or perform actions."
         - Cancel button: clears `userToDeactivate` state
         - Continue/Deactivate button: calls `handleToggleActive(userToDeactivate, true)` then clears state
       - For the "Activate" action (when user.isActive is false), keep the direct `handleToggleActive` call — no confirmation needed for non-destructive action.

    4. Do NOT change any other behavior — table columns, edit dialog, activate action all remain unchanged.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no type errors
    - `npm run build` succeeds
    - Grep for "AlertDialog" in user-table.tsx confirms it exists
    - Grep for "deactivateUser" confirms it's still called (but now through confirmation flow)
    - The direct `handleToggleActive(user.id, user.isActive)` call is no longer on the DropdownMenuItem for active users; instead it goes through state + AlertDialog
  </verify>
  <done>
    Clicking "Deactivate" on an active user opens an AlertDialog confirmation prompt showing the user's name and a warning. Clicking Cancel closes the dialog without deactivating. Clicking Deactivate proceeds with deactivation. Clicking "Activate" on an inactive user still works directly without confirmation.
  </done>
</task>

</tasks>

<verification>
- user-table.tsx imports and uses AlertDialog components
- Deactivation requires explicit confirmation via dialog
- Activation (non-destructive) remains direct
- No regressions in table rendering or edit functionality
</verification>

<success_criteria>
User deactivation is gated behind a confirmation dialog. The dialog shows the user's name and explains the consequence. Cancel aborts; confirm executes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-configuration-and-administration/07-06-SUMMARY.md`
</output>
