---
phase: 11-mobile-responsive-design
plan: 06
type: execute
wave: 1
depends_on: [11-01]
files_modified: [src/components/mobile/mobile-nav.tsx]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tapping 'Dashboard' tab in mobile navigation navigates to /dashboard page"
    - "Tapping 'My Requests' tab in mobile navigation navigates to /requests page (not /requests/my-requests)"
    - "Tapping 'Pending Approvals' tab in mobile navigation navigates to /requests/my-actions page (already correct)"
  artifacts:
    - path: "src/components/mobile/mobile-nav.tsx"
      provides: "Mobile navigation with correct route configuration"
      contains: "tabs array with corrected href values"
  key_links:
    - from: "src/components/mobile/mobile-nav.tsx"
      to: "src/app/(dashboard)/dashboard/page.tsx"
      via: "href='/dashboard'"
      pattern: "href: '/dashboard'"
    - from: "src/components/mobile/mobile-nav.tsx"
      to: "src/app/(dashboard)/requests/page.tsx"
      via: "href='/requests'"
      pattern: "href: '/requests'"
---

<objective>
Fix mobile navigation routes to point to correct existing pages

**Purpose:** Users reported that mobile navigation tabs navigate to 404 pages or incorrect routes. The Dashboard tab goes to /requests (should be /dashboard), and My Requests tab goes to /requests/my-requests (a 404). This plan corrects those href values.

**Output:** Mobile navigation with working tab routes - Dashboard → /dashboard, My Requests → /requests, Pending Approvals → /requests/my-actions
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mobile-responsive-design/11-01-SUMMARY.md
@.planning/phases/11-mobile-responsive-design/11-mobile-responsive-design-UAT.md

**UAT Gap (Test 4 - Tab Navigation):**
- "My Requests tab navigates to /requests/my-requests which is a 404 page. The route doesn't exist."
- "Dashboard tab goes to /requests (should be /dashboard)"
- "Pending Approvals goes to /requests/my-actions (correct)"

**Root Cause:**
Mobile navigation routes in mobile-nav.tsx (lines 19-20) were configured incorrectly during plan 11-01 implementation. The app uses:
- /dashboard - Tabbed dashboard interface with Pending/My Requests/All tabs
- /requests - Dedicated page for user's requests
- /requests/my-actions - Pending approvals requiring action (this is correct)

**Gap Reference:** .planning/phases/11-mobile-responsive-design/11-mobile-responsive-design-UAT.md
**Debug Session:** .planning/debug/mobile-nav-routes-broken.md
</context>

<tasks>

<task type="auto">
  <name>Fix Dashboard tab route</name>
  <files>src/components/mobile/mobile-nav.tsx</files>
  <action>
    Update the Dashboard tab href from '/requests' to '/dashboard' in the tabs array (line 19).

    Current (incorrect):
    { name: 'Dashboard', href: '/requests', icon: LayoutDashboard }

    Change to:
    { name: 'Dashboard', href: '/dashboard', icon: LayoutDashboard }

    This ensures the Dashboard tab navigates to the tabbed dashboard interface at /dashboard.
  </action>
  <verify>Build passes: npm run build</verify>
  <done>Dashboard tab href points to '/dashboard'</done>
</task>

<task type="auto">
  <name>Fix My Requests tab route</name>
  <files>src/components/mobile/mobile-nav.tsx</files>
  <action>
    Update the My Requests tab href from '/requests/my-requests' to '/requests' in the tabs array (line 20).

    Current (incorrect):
    { name: 'My Requests', href: '/requests/my-requests', icon: FileText }

    Change to:
    { name: 'My Requests', href: '/requests', icon: FileText }

    The route /requests/my-requests does not exist (404). The correct route is /requests which displays the user's requests with filters.
  </action>
  <verify>Build passes: npm run build</verify>
  <done>My Requests tab href points to '/requests' (existing route)</done>
</task>

</tasks>

<verification>
1. **Route validation:** All three tabs navigate to existing routes (no 404s)
2. **Build verification:** `npm run build` passes without errors
3. **Manual test (if desired):** On mobile viewport, click each tab and verify navigation
   - Dashboard tab → /dashboard (tabbed interface)
   - My Requests tab → /requests (dedicated requests page)
   - Pending Approvals tab → /requests/my-actions (unchanged, already correct)
</verification>

<success_criteria>
- [ ] Dashboard tab href changed from '/requests' to '/dashboard'
- [ ] My Requests tab href changed from '/requests/my-requests' to '/requests'
- [ ] Pending Approvals tab unchanged (already correct at '/requests/my-actions')
- [ ] Build passes without TypeScript or route errors
- [ ] UAT Test 4 (Tab Navigation) now passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-responsive-design/11-06-SUMMARY.md`
</output>
