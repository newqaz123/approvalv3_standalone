---
phase: 11-mobile-responsive-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/hooks/use-scroll-direction.tsx
  - tailwind.config.ts
  - src/components/mobile/mobile-nav.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Mobile top tab bar is visible on small screens (hidden on desktop)"
    - "Tab bar auto-hides when scrolling down and reappears when scrolling up"
    - "All 3 tabs navigate correctly: Dashboard, My Requests, Pending Approvals"
    - "Pending Approvals tab shows notification badge with count"
    - "User profile icon (first initial in circle) appears in top right corner"
  artifacts:
    - path: "package.json"
      contains: "\"vaul\""
    - path: "src/hooks/use-scroll-direction.tsx"
      provides: "Scroll direction detection hook for mobile nav"
      exports: ["useScrollDirection"]
    - path: "src/components/mobile/mobile-nav.tsx"
      provides: "Mobile top tab bar with smart scroll"
      min_lines: 80
    - path: "tailwind.config.ts"
      contains: "safe-area-inset-bottom"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/mobile/mobile-nav.tsx"
      via: "conditional import and render on mobile breakpoint"
      pattern: "hidden md:block|md:hidden"
---

<objective>
Install mobile dependencies and create the foundation for mobile navigation.

This plan sets up the infrastructure for mobile-responsive design by:
1. Installing Vaul for native-feeling mobile drawers/sheets
2. Creating a scroll direction hook for smart navbar behavior
3. Building the mobile top tab bar navigation with 3 tabs
4. Configuring Tailwind for mobile safe areas

Purpose: Establish the mobile navigation infrastructure that all other mobile features will depend on.
Output: Working mobile navigation component with smart scroll behavior, ready for integration into the app layout.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-mobile-responsive-design/11-CONTEXT.md
@.planning/phases/11-mobile-responsive-design/11-RESEARCH.md
@src/components/navigation/navbar.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Install mobile dependencies and configure Tailwind</name>
  <files>package.json, tailwind.config.ts</files>
  <action>
    1. Run `npm install vaul` to add the drawer library
    2. Update tailwind.config.ts to add:
       - Custom utilities for safe-area-inset support (pb-safe, pt-safe)
       - Animation utilities for smooth scroll behavior
    3. Verify installation by checking package.json has "vaul" dependency

    Per RESEARCH.md, Vaul is the standard for iOS-like drawers in React/Radix ecosystem.
    Do NOT install any alternative drawer libraries.
  </action>
  <verify>grep -q '"vaul"' package.json && ls node_modules/vaul/package.json</verify>
  <done>Vaul package installed and Tailwind configured for mobile safe areas</done>
</task>

<task type="auto">
  <name>Create scroll direction hook for smart navbar</name>
  <files>src/hooks/use-scroll-direction.tsx</files>
  <action>
    Create src/hooks/use-scroll-direction.tsx with the following implementation:
    - Returns `isVisible` boolean state
    - Tracks scroll position with useState and useEffect
    - Auto-hides navbar when scrolling down (like Facebook)
    - Shows navbar when scrolling up
    - Keeps navbar visible when near top (scrollY < 10)
    - Properly cleans up event listener

    Use the pattern from RESEARCH.md code examples.
    Include throttling for performance (requestAnimationFrame).
  </action>
  <verify>test -f src/hooks/use-scroll-direction.tsx && grep -q "useScrollDirection" src/hooks/use-scroll-direction.tsx</verify>
  <done>Scroll direction hook created with Facebook-style smart scroll behavior</done>
</task>

<task type="auto">
  <name>Create mobile top tab bar navigation component</name>
  <files>src/components/mobile/mobile-nav.tsx</files>
  <action>
    Create src/components/mobile/mobile-nav.tsx implementing:
    - Top fixed position tab bar (z-50)
    - 3 tabs: Dashboard (/dashboard), My Requests (/requests), Pending Approvals (/requests/my-actions)
    - Smart scroll visibility using useScrollDirection hook
    - Active tab indication with different background color (bg-gray-100)
    - User profile icon (first initial in colored circle) in top right
    - Notification badge on Pending Approvals tab (red circle with count)
    - Each tab triggers full page navigation (Link component)
    - Mobile-only visibility (hidden on md+ breakpoints)

    Styling requirements per CONTEXT.md:
    - White background with bottom border
    - Smooth transition for hide/show (duration-300)
    - Tab text should be touch-friendly (minimum 44px tap target)
    - Use Lucide icons for visual clarity (LayoutDashboard, FileText, Bell)

    DO NOT implement hamburger menu or sidebar navigation.
  </action>
  <verify>test -f src/components/mobile/mobile-nav.tsx && grep -q "useScrollDirection" src/components/mobile/mobile-nav.tsx</verify>
  <done>Mobile navigation component with 3 tabs, smart scroll, and notification badge</done>
</task>

<task type="auto">
  <name>Integrate mobile nav into dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
    Update src/app/(dashboard)/layout.tsx to:
    - Import MobileNav component
    - Render MobileNav on mobile breakpoints (hidden md:flex)
    - Keep existing Navbar on desktop (md:flex hidden)
    - Ensure both navs don't render simultaneously (use responsive classes)
    - Add top padding to main content on mobile to account for fixed nav (pt-16)

    Layout structure should be:
    - Mobile: fixed top nav + main content with top padding
    - Desktop: existing layout unchanged

    DO NOT modify the desktop navbar behavior.
  </action>
  <verify>grep -q "MobileNav" src/app/(dashboard)/layout.tsx && grep -q "hidden md:" src/app/(dashboard)/layout.tsx</verify>
  <done>Layout conditionally renders mobile nav on small screens, desktop nav on large screens</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Run `npm run build` - should complete without errors
2. View app on mobile viewport (375px width) - top tab bar should be visible
3. Scroll down on mobile - nav bar should hide
4. Scroll up on mobile - nav bar should reappear
5. Click each tab - should navigate to correct page
</verification>

<success_criteria>
- Vaul dependency installed
- Mobile navigation visible on mobile breakpoints only
- Smart scroll behavior working (hide on scroll down, show on scroll up)
- All 3 tabs navigate correctly
- Notification badge appears on Pending Approvals tab
- Desktop navigation unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-responsive-design/11-01-SUMMARY.md`
</output>
