---
phase: 11-mobile-responsive-design
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - src/app/globals.css
  - src/components/mobile/admin-card.tsx
  - src/components/admin/user-table.tsx
  - src/components/admin/department-table.tsx
  - src/components/admin/template-table.tsx
  - src/components/dashboard/activity-timeline.tsx
  - src/components/ui/button.tsx
autonomous: true

must_haves:
  truths:
    - "Body text is 16px minimum on mobile (text-base)"
    - "All interactive elements have 44x44px minimum tap targets"
    - "Admin tables use card view on mobile"
    - "Activity timeline is compact on mobile"
    - "Viewport uses 100dvh to avoid mobile browser quirks"
  artifacts:
    - path: "src/app/globals.css"
      contains: "min-height.*100dvh|text-base.*min-width"
    - path: "src/components/mobile/admin-card.tsx"
      provides: "Generic card component for admin tables on mobile"
      exports: ["AdminCard"]
    - path: "src/components/admin/user-table.tsx"
      contains: "AdminCard|md:hidden.*card"
    - path: "src/components/dashboard/activity-timeline.tsx"
      contains: "mobile.*compact|hidden.*sm:block"
  key_links:
    - from: "src/app/globals.css"
      to: "tailwind.config.ts"
      via: "base styles and global utilities"
      pattern: "@layer base.*body"
    - from: "src/components/admin/user-table.tsx"
      to: "src/components/mobile/admin-card.tsx"
      via: "generic card component for admin data"
      pattern: "AdminCard"
---

<objective>
Apply global mobile-responsive styles and adapt remaining views.

This plan ensures all remaining views are mobile-friendly by:
1. Setting base mobile typography (16px minimum)
2. Enforcing tap target sizes globally
3. Converting admin tables to card views
4. Making activity timeline compact on mobile
5. Using 100dvh for full-height elements

Purpose: Complete mobile responsiveness across all views with consistent
typography and touch targets.
Output: Fully mobile-responsive application with consistent UX patterns.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-mobile-responsive-design/11-CONTEXT.md
@.planning/phases/11-mobile-responsive-design/11-RESEARCH.md
@src/app/globals.css
@src/components/admin/user-table.tsx
@src/components/admin/department-table.tsx
@src/components/admin/template-table.tsx
@src/components/dashboard/activity-timeline.tsx
</context>

<tasks>

<task type="auto">
  <name>Add global mobile-responsive base styles</name>
  <files>src/app/globals.css</files>
  <action>
    Update src/app/globals.css:

    1. Base typography (prevent iOS zoom per RESEARCH.md):
       ```css
       @layer base {
         body {
           @apply text-base min-w-[320px];
         }
       }
       ```

    2. Mobile viewport height fix (100dvh per RESEARCH.md):
       ```css
       .h-screen-mobile {
         height: 100dvh;
         min-height: 100dvh;
       }
       ```

    3. Tap target utility (44x44px minimum):
       ```css
       .tap-target {
         @apply min-h-[44px] min-w-[44px];
       }
       ```

    4. Safe area utilities:
       ```css
       .pb-safe {
         padding-bottom: env(safe-area-inset-bottom);
       }
       .pt-safe {
         padding-top: env(safe-area-inset-top);
       }
       ```

    5. Touch feedback:
       ```css
       .touch-feedback {
         @apply active:bg-gray-100 active:scale-[0.98] transition-transform;
       }
       ```

    These utilities can be used throughout the app.
  </action>
  <verify>grep -q "text-base.*min-w-\[320px\]\|min-h-\[44px\]" src/app/globals.css</verify>
  <done>Global CSS with mobile typography, tap targets, and safe areas</done>
</task>

<task type="auto">
  <name>Convert admin tables to mobile card views</name>
  <files>src/components/mobile/admin-card.tsx, src/components/admin/user-table.tsx, src/components/admin/department-table.tsx, src/components/admin/template-table.tsx</files>
    - Create src/components/mobile/admin-card.tsx (reusable admin card)
    - Update user-table.tsx: show cards on mobile, table on desktop
    - Update department-table.tsx: show cards on mobile, table on desktop
    - Update template-table.tsx: show cards on mobile, table on desktop
    - Card layout: title, key details, status badge, action menu
    - Action menu: three-dot menu with Edit/Delete options
    - Use same responsive pattern as request tables

    Per CONTEXT.md: "Admin tables use consistent card view approach"

    Cards should show:
    - User: name, email, role, department, active status
    - Department: name, user count, hierarchy link
    - Template: name, isDefault badge, description preview

    DO NOT create separate card for each table - use generic AdminCard component.
  </action>
  <verify>grep -q "AdminCard\|md:hidden.*card" src/components/admin/user-table.tsx</verify>
  <done>Admin tables display cards on mobile, tables on desktop</done>
</task>

<task type="auto">
  <name>Make activity timeline compact on mobile</name>
  <files>src/components/dashboard/activity-timeline.tsx</files>
  <action>
    Update src/components/dashboard/activity-timeline.tsx:
    - On mobile: compact timeline with smaller icons and text
    - Hide less important details on mobile (e.g., exact timestamps, verbose descriptions)
    - Tap timeline item to expand full details (optional)
    - Use responsive classes: text-sm on mobile, text-base on desktop
    - Reduce spacing between items on mobile (space-y-2 vs space-y-4)
    - Hide vertical connector line on mobile (use spacing instead)

    Mobile layout:
    ```
    [Icon] [User] [Action]           [Relative time]
          [Truncated description...]
    ```

    Desktop layout (existing):
    ```
    [Icon] | [User] [Action] - [Full description]
           | [Timestamp]
    ```

    Use existing StatusBadge and icon components.
  </action>
  <verify>grep -q "md:hidden\|hidden.*sm:\|mobile.*compact" src/components/dashboard/activity-timeline.tsx</verify>
  <done>Activity timeline compact on mobile with expandable details</done>
</task>

<task type="auto">
  <name>Apply mobile styles to buttons and interactive elements</name>
  <files>src/components/ui/button.tsx</files>
  <action>
    Update src/components/ui/button.tsx for better mobile experience:
    - Ensure default button has min-h-10 (40px) for touch
    - Add h-11 to large variant
    - Add active:scale-[0.98] for touch feedback
    - Add transition-all for smooth animations
    - Ensure icon buttons (icon-only) have min-w-11

    Button variants should maintain desktop appearance while
    being mobile-friendly.

    Apply tap-target class from globals.css where appropriate.

    DO NOT change button variant colors or semantics.
  </action>
  <verify>grep -q "min-h-\[44px\]\|min-h-10\|active:scale" src/components/ui/button.tsx</verify>
  <done>Buttons meet mobile tap target requirements with touch feedback</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Check all admin pages on mobile - tables should be cards
2. Check activity timeline on mobile - should be compact
3. Measure button tap targets - all 44x44px minimum
4. Check text size - body text is 16px minimum
5. Test on iOS - safe areas respected, no zoom on input focus
</verification>

<success_criteria>
- Global styles enforce 16px minimum text
- Tap targets are 44x44px minimum across app
- Admin tables use card views on mobile
- Activity timeline is compact on mobile
- 100dvh used for full-height elements
- All views work on 320px-768px screens
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-responsive-design/11-05-SUMMARY.md`
</output>
