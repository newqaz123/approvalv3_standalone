---
phase: 11-mobile-responsive-design
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/components/requests/request-form.tsx
  - src/components/mobile/mobile-file-upload.tsx
autonomous: true

must_haves:
  truths:
    - "Request form uses single-column stacked layout on mobile"
    - "File upload button triggers camera or file picker on mobile"
    - "All inputs use 16px font size to prevent iOS zoom"
    - "Tap targets meet minimum 44x44px requirement"
    - "Template selector works on mobile"
  artifacts:
    - path: "src/components/mobile/mobile-file-upload.tsx"
      provides: "Mobile file upload with camera trigger"
      min_lines: 80
    - path: "src/components/requests/request-form.tsx"
      contains: "text-base.*md:text-sm|min-h-\\[44px\\]|capture"
  key_links:
    - from: "src/components/requests/request-form.tsx"
      to: "src/components/mobile/mobile-file-upload.tsx"
      via: "conditional render on mobile breakpoint"
      pattern: "MobileFileUpload.*input.*capture"
---

<objective>
Make the request creation form mobile-friendly with camera support and proper
input sizing.

This plan adapts the request form for mobile use by ensuring proper input
sizes (to prevent iOS zoom), enabling camera capture for file uploads,
and using a stacked single-column layout that works well on small screens.

Purpose: Enable users to create requests on mobile with working file uploads
including camera capture, and without iOS auto-zoom issues.
Output: Mobile-optimized request form with camera file upload.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-mobile-responsive-design/11-CONTEXT.md
@.planning/phases/11-mobile-responsive-design/11-RESEARCH.md
@src/components/requests/request-form.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
</context>

<tasks>

<task type="auto">
  <name>Create mobile file upload component with camera support</name>
  <files>src/components/mobile/mobile-file-upload.tsx</files>
  <action>
    Create src/components/mobile/mobile-file-upload.tsx:
    - Two upload buttons on mobile:
      * "Take Photo" - triggers camera (capture="environment")
      * "Choose File" - opens file picker
    - Hidden file inputs with capture attribute:
      * `<input type="file" accept="image/*" capture="environment" id="camera">`
      * `<input type="file" accept="*" id="files">`
    - Labels with large touch targets (min-h-12, w-full)
    - Icons: Camera for photo, Upload for file picker
    - Selected files display as cards with thumbnail/preview
    - Remove button on each file
    - File size display under each filename
    - Description input under each file (collapsible)

    Per RESEARCH.md mobile file upload pattern:
    - Use accept="image/*" + capture="environment" for back camera
    - Use accept="image/*" + capture="user" for front camera (selfies)
    - Separate inputs for camera vs file picker

    Handle file preview with URL.createObjectURL for images.
  </action>
  <verify>test -f src/components/mobile/mobile-file-upload.tsx && grep -q "capture" src/components/mobile/mobile-file-upload.tsx</verify>
  <done>Mobile file upload with camera capture and file picker</done>
</task>

<task type="auto">
  <name>Update request form for mobile responsiveness</name>
  <files>src/components/requests/request-form.tsx</files>
  <action>
    Modify src/components/requests/request-form.tsx:

    1. Layout changes:
       - Single column stacked layout (existing is mostly good)
       - Full-width inputs on mobile (w-full)
       - Add responsive spacing (space-y-4 md:space-y-6)

    2. Input sizing (prevent iOS zoom per RESEARCH.md):
       - Add text-base class to all inputs (16px minimum)
       - Pattern: `className="text-base md:text-sm ..."`
       - Update Input, Textarea, Select components if needed

    3. Tap targets (44x44px minimum):
       - Ensure all buttons have min-h-11 or h-11
       - Add padding to small clickable elements
       - Template selector: full-width SelectTrigger with h-11

    4. File upload section:
       - Import MobileFileUpload component
       - Render on mobile (md:hidden)
       - Keep existing FileUploadZone on desktop (hidden md:flex)
       - Both use same state management (selectedFiles)

    5. Template selector:
       - Ensure dropdown is touch-friendly
       - Consider bottom sheet for template list on mobile

    6. Form buttons:
       - Stack vertically on mobile (flex-col gap-2)
       - Full-width buttons (w-full)
       - Side-by-side on desktop (md:flex-row)

    DO NOT change form validation or submission logic.
  </action>
  <verify>grep -q "text-base.*md:text-sm\|MobileFileUpload" src/components/requests/request-form.tsx</verify>
  <done>Request form mobile-optimized with proper input sizes and camera upload</done>
</task>

<task type="auto">
  <name>Update form UI components for mobile</name>
  <files>src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/select.tsx</files>
  <action>
    Update UI components for better mobile experience:

    1. src/components/ui/input.tsx:
       - Add default min-h-11 class for touch targets
       - Ensure text-base can be passed or set as default

    2. src/components/ui/textarea.tsx:
       - Add min-h-24 for mobile (tappable area)
       - Ensure resize-y works on touch

    3. src/components/ui/select.tsx:
       - SelectTrigger should have min-h-11
       - Ensure dropdown is touch-friendly (large items)

    These changes affect all forms, not just request form.

    Focus on minimum tap target size without breaking desktop styling.
    Use responsive classes where needed (e.g., min-h-10 md:min-h-9).
  </action>
  <verify>grep -q "min-h-11\|min-h-10" src/components/ui/input.tsx && grep -q "min-h-11" src/components/ui/select.tsx</verify>
  <done>Form UI components have mobile-friendly tap targets</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Open /requests/new on mobile - form should be full-width, single column
2. Focus on text input - iOS should NOT zoom in
3. Tap "Take Photo" - camera should open
4. Tap "Choose File" - file picker should open
5. Submit form - should work same as desktop
6. All buttons tappable without zooming
</verification>

<success_criteria>
- Form uses single column layout on mobile
- Inputs are 16px font (no iOS zoom)
- Camera upload works
- Tap targets are 44x44px minimum
- Template selector functional on mobile
- Desktop form unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/11-mobile-responsive-design/11-04-SUMMARY.md`
</output>
