---
phase: 09-docker-deployment-infrastructure
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - docs/DEPLOY.md
  - README.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Deployment documentation is complete"
    - "Manual verification confirms system works"
  artifacts:
    - path: docs/DEPLOY.md
      contains: "Hostinger"
  key_links:
    - from: README.md
      to: docs/DEPLOY.md
      via: "Deployment Guide link"
---

<objective>
Document deployment procedures and verify the infrastructure stack locally.

Purpose: Ensure the "one-command" promise is true and documented for the user.
Output: DEPLOY.md and a verified running system.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-docker-deployment-infrastructure/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create Deployment Documentation</name>
  <files>docs/DEPLOY.md, README.md</files>
  <action>
    1. Create `docs/DEPLOY.md`:
       - Prerequisites (Docker, Git).
       - Initial Setup (Clone, Setup Script, Env Vars).
       - Deployment (Deploy Script).
       - Updates (Deploy Script).
       - Backups (Backup Script & Cron setup).
       - Troubleshooting (Logs, Shell access).
       
    2. Update `README.md`:
       - Add "Deployment" section linking to `docs/DEPLOY.md`.
  </action>
  <verify>
    test -f docs/DEPLOY.md
  </verify>
  <done>
    Documentation covers all lifecycle phases.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Docker Deployment Infrastructure</what-built>
  <how-to-verify>
    1. Run `cp .env .env.production` (fill with real or dummy values).
    2. Run `./scripts/deploy.sh` (or `docker compose up -d --build`).
    3. Verify containers are running: `docker compose ps`.
    4. Access `http://localhost:3000`.
    5. Check logs for migration success: `docker compose logs migrate`.
    6. Upload a file, restart containers, verify file persists.
  </how-to-verify>
  <resume-signal>Type "verified" if deployment succeeds locally</resume-signal>
</task>

</tasks>

<verification>
User manual verification of the stack.
</verification>

<success_criteria>
- [ ] DEPLOY.md provides step-by-step instructions
- [ ] System successfully boots in Docker locally
- [ ] Data persistence validated (DB + Uploads)
</success_criteria>

<output>
After completion, create `.planning/phases/09-docker-deployment-infrastructure/09-04-SUMMARY.md`
</output>
