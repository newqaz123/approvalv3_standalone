---
phase: 09-docker-deployment-infrastructure
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - src/app/api/health/route.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All services start automatically and pass health checks"
    - "Health monitoring and status verification works correctly"
  artifacts:
    - path: "src/app/api/health/route.ts"
      provides: "HTTP health endpoint for container health checks"
      min_lines: 30
    - path: "Dockerfile"
      provides: "Docker image with curl installed for health checks"
      contains: "apk add --no-cache curl"
    - path: "docker-compose.yml"
      provides: "Updated healthcheck using curl"
      contains: "curl.*localhost:3000/api/health"
  key_links:
    - from: "docker-compose.yml"
      to: "src/app/api/health/route.ts"
      via: "healthcheck command"
      pattern: "curl.*api/health"
    - from: "Dockerfile"
      to: "docker-compose.yml"
      via: "provides curl binary for healthcheck"
      pattern: "apk add.*curl"
---

<objective>
Implement health monitoring endpoint and fix health check tools to enable container health checks

Purpose: Close gaps from Phase 9 verification - missing /api/health endpoint and missing curl/wget tools
Output: Working health checks in Docker Compose and health-check.sh script
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-docker-deployment-infrastructure/09-04-SUMMARY.md
@.planning/phases/09-docker-deployment-infrastructure/09-04-VERIFICATION.md

# Gap context from VERIFICATION.md:
# - Gap 1: /api/health endpoint doesn't exist
# - Gap 1: wget is not installed in Docker container
# - Gap 2: health-check.sh fails due to missing endpoint
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curl to Dockerfile and update healthcheck command</name>
  <files>Dockerfile, docker-compose.yml</files>
  <action>
  1. Modify Dockerfile:
     - In the base stage (line 6), add curl to the apk add command:
       - Change: `RUN apk add --no-cache libc6-compat openssl`
       - To: `RUN apk add --no-cache libc6-compat openssl curl`
     - This ensures curl is available in all subsequent stages

  2. Modify docker-compose.yml:
     - Update the app service healthcheck (line 79) to use curl instead of wget:
       - Change: `test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1"]`
       - To: `test: ["CMD-SHELL", "curl -f http://localhost:3000/api/health || exit 1"]`

  Reference: scripts/health-check.sh already uses curl (line 136), so switching healthcheck to curl is consistent
  Gap reason: VERIFICATION.md - "wget is not installed in Docker container" and "healthcheck references /api/health endpoint that doesn't exist"
  </action>
  <verify>grep -q 'apk add --no-cache libc6-compat openssl curl' Dockerfile && grep -q 'curl -f http://localhost:3000/api/health' docker-compose.yml</verify>
  <done>Dockerfile installs curl, docker-compose.yml healthcheck uses curl instead of wget</done>
</task>

<task type="auto">
  <name>Task 2: Create /api/health endpoint</name>
  <files>src/app/api/health/route.ts</files>
  <action>
  Create src/app/api/health/route.ts with a simple health check endpoint:

  1. Handle GET requests (no auth required - health checks run from inside container)
  2. Check database connectivity using Prisma:
     - Run a simple query: `await prisma.$queryRaw`SELECT 1``
     - This verifies database connection without complex queries
  3. Return JSON response with:
     - status: "ok" (always return ok, include database health separately)
     - timestamp: ISO 8601 timestamp
     - database: "connected" or "error: <reason>" (informative, doesn't affect status code)
  4. Always return HTTP 200 on success (endpoint exists and responds)
  5. Return HTTP 500 only if unhandled error occurs

  Pattern: Follow Next.js API route pattern from src/app/api/force-refresh/route.ts
  - Use NextResponse.json() for responses
  - Wrap in try/catch for error handling
  - Export GET function

  Gap reason: VERIFICATION.md - "/api/health endpoint doesn't exist"

  Example structure:
  ```typescript
  import { NextResponse } from 'next/server'
  import { prisma } from '@/lib/prisma'

  export async function GET() {
    try {
      // Check database connectivity
      await prisma.$queryRaw`SELECT 1`
      const dbStatus = 'connected'
    } catch (error) {
      const dbStatus = 'error'
    }
    return NextResponse.json({ status: 'ok', timestamp: new Date().toISOString(), database: dbStatus })
  }
  ```
  </action>
  <verify>test -f src/app/api/health/route.ts && grep -q "export async function GET" src/app/api/health/route.ts && grep -q "prisma" src/app/api/health/route.ts</verify>
  <done>Health endpoint exists at /api/health and returns JSON with status and database connectivity</done>
</task>

</tasks>

<verification>
After completing tasks, verify gap closure:

1. Build Docker image: `docker compose build`
2. Start services: `docker compose up -d`
3. Wait for services to be healthy: `docker compose ps` (app and db should show "healthy")
4. Test health endpoint from host: `curl http://localhost:3000/api/health`
   - Expected: `{"status":"ok","timestamp":"...","database":"connected"}`
5. Run health-check.sh: `./scripts/health-check.sh`
   - Expected: All checks pass, exit code 0

Expected results:
- Docker build completes without errors
- App container becomes healthy (healthcheck passes)
- Health endpoint responds with 200 and JSON
- health-check.sh reports all systems healthy
</verification>

<success_criteria>
Gap closure complete when:
- [ ] src/app/api/health/route.ts exists and responds with JSON
- [ ] Dockerfile installs curl in base stage
- [ ] docker-compose.yml healthcheck uses curl instead of wget
- [ ] App container passes healthcheck (shows "healthy" in `docker compose ps`)
- [ ] health-check.sh script executes successfully and reports all systems healthy
</success_criteria>

<output>
After completion, create `.planning/phases/09-docker-deployment-infrastructure/09-05-SUMMARY.md`
</output>
