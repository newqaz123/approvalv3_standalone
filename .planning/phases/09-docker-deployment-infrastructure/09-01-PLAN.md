---
phase: 09-docker-deployment-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - next.config.mjs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Docker build completes successfully"
    - "Image size is optimized (multi-stage)"
    - "Standalone output is enabled in Next.js"
  artifacts:
    - path: Dockerfile
      contains: "FROM node:20-alpine AS runner"
    - path: .dockerignore
      contains: "node_modules"
    - path: next.config.mjs
      contains: "output: 'standalone'"
  key_links:
    - from: Dockerfile
      to: next.config.mjs
      via: "COPY --from=builder /app/.next/standalone ./"
---

<objective>
Configure Next.js for Docker deployment and create a production-optimized multi-stage Dockerfile.

Purpose: Enable containerized deployment with minimal image size (~110MB) using Next.js standalone mode.
Output: Valid Dockerfile and updated project config ready for building.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-docker-deployment-infrastructure/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Configure Next.js Standalone Output</name>
  <files>next.config.mjs</files>
  <action>
    Modify `next.config.mjs` to enable `output: 'standalone'`.
    Ensure the config is compatible with Docker build context (no absolute paths dependent on host).
  </action>
  <verify>
    grep "output: 'standalone'" next.config.mjs
  </verify>
  <done>
    Next.js config explicitly enables standalone mode.
  </done>
</task>

<task type="auto">
  <name>Create .dockerignore</name>
  <files>.dockerignore</files>
  <action>
    Create `.dockerignore` to exclude:
    - node_modules
    - .next
    - .git
    - .env* (secrets should not be baked in, except public ones if needed, but safe to exclude all and mount)
    - uploads/
    - tests/
    - README.md
    - .planning/
  </action>
  <verify>
    cat .dockerignore | grep "node_modules"
  </verify>
  <done>
    Build context excludes unnecessary files.
  </done>
</task>

<task type="auto">
  <name>Create Multi-Stage Dockerfile</name>
  <files>Dockerfile</files>
  <action>
    Create `Dockerfile` with the following stages:
    1. `base`: node:20-alpine, libc6-compat, openssl
    2. `deps`: install dependencies (npm ci)
    3. `builder`: build the app (npx prisma generate && npm run build)
    4. `runner`: minimal production image
       - Create non-root user (nextjs/nodejs)
       - Copy public, .next/static, and .next/standalone
       - Set permissions for .next and uploads directory
       - Expose port 3000
       - CMD ["node", "server.js"]
    
    Ensure `PRISMA_CLI_BINARY_TARGETS` is handled if necessary (usually auto-detected, but alpine needs linux-musl-openssl-3.0.x).
  </action>
  <verify>
    docker build -t approval-app:test .
  </verify>
  <done>
    Docker build succeeds and produces a functional image.
  </done>
</task>

</tasks>

<verification>
Run `docker run --rm approval-app:test node -v` to confirm Node version.
</verification>

<success_criteria>
- [ ] Next.js configured for standalone output
- [ ] .dockerignore prevents secret/bloat leakage
- [ ] Docker build command succeeds
- [ ] Image contains necessary Prisma binaries
</success_criteria>

<output>
After completion, create `.planning/phases/09-docker-deployment-infrastructure/09-01-SUMMARY.md`
</output>
