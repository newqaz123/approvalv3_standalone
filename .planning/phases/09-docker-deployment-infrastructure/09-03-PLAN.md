---
phase: 09-docker-deployment-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - scripts/setup.sh
  - scripts/deploy.sh
  - scripts/backup.sh
  - scripts/restore.sh
  - scripts/health-check.sh
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Setup script prepares environment"
    - "Deploy script handles updates with zero data loss"
    - "Backup script captures DB and files"
    - "Health check script verifies service status"
  artifacts:
    - path: scripts/setup.sh
      contains: "chmod"
    - path: scripts/deploy.sh
      contains: "docker compose up"
    - path: scripts/health-check.sh
      contains: "docker compose ps"
  key_links:
    - from: scripts/deploy.sh
      to: docker-compose.yml
      via: "docker compose command"
---

<objective>
Create lifecycle management scripts for setup, deployment, backups, restoration, and health monitoring.

Purpose: Simplify server management to single commands, preventing manual error.
Output: Executable bash scripts in `scripts/` directory.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-docker-deployment-infrastructure/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create Setup and Deploy Scripts</name>
  <files>scripts/setup.sh, scripts/deploy.sh</files>
  <action>
    1. `scripts/setup.sh`:
       - Create `uploads` directory (if using bind mount) or ensure volume permissions.
       - Copy `.env.production.example` to `.env` if not exists.
       - Make scripts executable.
       
    2. `scripts/deploy.sh`:
       - Pull latest changes (git pull).
       - Rebuild images: `docker compose build`.
       - Start services: `docker compose up -d`.
       - Prune old images to save space.
  </action>
  <verify>
    chmod +x scripts/*.sh && ls -l scripts/
  </verify>
  <done>
    Scripts exist and are executable.
  </done>
</task>

<task type="auto">
  <name>Create Backup and Restore Scripts</name>
  <files>scripts/backup.sh, scripts/restore.sh</files>
  <action>
    1. `scripts/backup.sh`:
       - Timestamped backup creation.
       - `docker compose exec` to run `pg_dump` inside db container.
       - Backup `uploads` volume (using helper container or tar if bind mounted).
       - Retention policy (keep last 5 backups).
       
    2. `scripts/restore.sh`:
       - Accept backup file path as argument.
       - Restore DB via `psql`.
       - Restore uploads.
  </action>
  <verify>
    test -f scripts/backup.sh
  </verify>
  <done>
    Backup system automated.
  </done>
</task>

<task type="auto">
  <name>Create Health Check Script</name>
  <files>scripts/health-check.sh</files>
  <action>
    Create `scripts/health-check.sh` that:
    - Checks if `docker compose` services are running.
    - Checks `app` container health (curl localhost:3000/api/health or similar, or just container status).
    - Checks `db` container status.
    - Returns exit code 0 if all healthy, non-zero otherwise.
    - Can be used by monitoring tools or cron.
  </action>
  <verify>
    chmod +x scripts/health-check.sh && ls -l scripts/health-check.sh
  </verify>
  <done>
    Health check script exists and is executable.
  </done>
</task>

</tasks>

<verification>
Verify script syntax with `bash -n scripts/*.sh`.
</verification>

<success_criteria>
- [ ] setup.sh prepares host environment
- [ ] deploy.sh handles build and restart
- [ ] backup.sh safely dumps database and files
- [ ] health-check.sh verifies deployment status
- [ ] All scripts are executable
</success_criteria>

<output>
After completion, create `.planning/phases/09-docker-deployment-infrastructure/09-03-SUMMARY.md`
</output>
