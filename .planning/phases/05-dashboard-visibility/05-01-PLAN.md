---
phase: 05-dashboard-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/dashboard/dashboard-tabs.tsx
autonomous: true
must_haves:
  truths:
    - User sees three tabbed views ("Pending My Approval", "My Requests", "All Requests")
    - Default tab is "Pending My Approval" on page load
    - Tab switching changes view without page reload
    - Tab labels have no count badges
  artifacts:
    - path: src/app/dashboard/page.tsx
      provides: "Dashboard page entry point"
      min_lines: 30
    - path: src/components/dashboard/dashboard-tabs.tsx
      provides: "Tab navigation component"
      min_lines: 80
    - path: src/components/ui/tabs.tsx
      provides: "shadcn/ui Tabs component"
      contains: "TabsList"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/dashboard/dashboard-tabs.tsx"
      via: "import DashboardTabs"
      pattern: "import.*DashboardTabs"
    - from: "src/components/dashboard/dashboard-tabs.tsx"
      to: "@/components/ui/tabs"
      via: "shadcn/ui tabs component"
      pattern: "from '@/components/ui/tabs'"
---

<objective>
Create dashboard page with three tabbed views for request discovery
Purpose: Provides entry point for users to find and track relevant requests
Output: Working `/dashboard` route with tab navigation showing three placeholder table views
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked Decisions from 05-CONTEXT.md:
- Single `/dashboard` page with tab navigation at the top (not separate routes or sidebar)
- Default tab is "Pending My Approval" — action-oriented entry point
- Always default to Pending tab (no session memory for tab selection)
- Tab labels without count badges — simpler, no counting query overhead

# From 05-RESEARCH.md:
- Use shadcn/ui Tabs component (install via `npx shadcn@latest add tabs`)
- Client-side tab switching with useState for active tab
- Default value hard-coded to "pending" (no persistence per CONTEXT.md)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn/ui tabs component</name>
  <files>src/components/ui/tabs.tsx</files>
  <action>
    Install shadcn/ui tabs component using the CLI:
    ```
    npx shadcn@latest add tabs
    ```
    This creates src/components/ui/tabs.tsx with Tabs, TabsList, TabsTrigger, TabsContent components.
    Confirm file exists after installation.
  </action>
  <verify>ls -la src/components/ui/tabs.tsx shows the file exists</verify>
  <done>tabs.tsx component installed and ready to use</done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard page route</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Create src/app/dashboard/page.tsx as a Server Component that:
    - Fetches current user data using { userId } from auth()
    - Passes userId to DashboardTabs client component
    - Returns basic page layout with header and DashboardTabs component
    - Page title: "Dashboard"
    - No data fetching in this task (that comes in 05-02)
    - Use standard shadcn/ui container/padding patterns

    Structure:
    ```tsx
    import { auth } from '@clerk/nextjs/server'
    import { DashboardTabs } from '@/components/dashboard/dashboard-tabs'

    export default async function DashboardPage() {
      const { userId } = await auth()
      return (
        <div className="container py-6">
          <h1 className="text-2xl font-bold mb-6">Dashboard</h1>
          <DashboardTabs userId={userId} />
        </div>
      )
    }
    ```
  </action>
  <verify>Page renders at /dashboard with header and tabs visible</verify>
  <done>Dashboard page accessible with tab navigation component rendered</done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardTabs component with three tabs</name>
  <files>src/components/dashboard/dashboard-tabs.tsx</files>
  <action>
    Create src/components/dashboard/dashboard-tabs.tsx as a client component that:
    - Uses shadcn/ui Tabs, TabsList, TabsTrigger, TabsContent
    - Manages activeTab state with default value "pending" (hard-coded, no persistence)
    - Defines three tabs: "pending", "my-requests", "all"
    - Tab labels: "Pending My Approval", "My Requests", "All Requests" (no count badges)
    - Each TabsContent renders a placeholder div for now (tables added in 05-02)
    - Accepts userId prop (will be used for data fetching in 05-02)

    Per CONTEXT.md requirement: "Always default to Pending tab (no session memory for tab selection)"
    - Do NOT use localStorage or URL params for tab state
    - Reset to "pending" on every page load

    Placeholder content for each tab:
    ```tsx
    <TabsContent value="pending" className="mt-4">
      <div className="p-8 text-center text-gray-500">
        Pending My Approval view — loading...
      </div>
    </TabsContent>
    ```

    Use flex layout for TabsList to ensure proper spacing.
  </action>
  <verify>Clicking tabs switches content, default tab is "Pending My Approval"</verify>
  <done>Three tabs visible, clickable, default to Pending, no count badges</done>
</task>

</tasks>

<verification>
1. Visit /dashboard route - page loads without errors
2. Default tab shown is "Pending My Approval"
3. Clicking "My Requests" tab switches view to that content
4. Clicking "All Requests" tab switches view to that content
5. No count badges visible on tab labels
6. Refreshing page resets tab to "Pending My Approval"
7. No console errors related to tabs or missing components
</verification>

<success_criteria>
- Dashboard accessible at /dashboard route
- Three tabs displayed with correct labels
- Tab switching works client-side without page reload
- Default tab is always "Pending My Approval" on page load
- No count badges on tab labels
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-visibility/05-01-SUMMARY.md`
</output>
