---
phase: 04.1-auto-refresh-overlay-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/skeleton.tsx
  - src/components/loading/request-detail-skeleton.tsx
  - src/components/requests/stale-data-banner.tsx
autonomous: true

must_haves:
  truths:
    - "Loading state shows skeleton shimmer instead of spinner"
    - "Skeleton layout matches modal content structure"
    - "Warning banner displays amber/yellow styling with refresh button"
  artifacts:
    - path: "src/components/ui/skeleton.tsx"
      provides: "Skeleton base component"
      min_lines: 10
    - path: "src/components/loading/request-detail-skeleton.tsx"
      provides: "Modal skeleton layout"
      min_lines: 40
    - path: "src/components/requests/stale-data-banner.tsx"
      provides: "Stale data warning component"
      min_lines: 20
  key_links:
    - from: "src/components/loading/request-detail-skeleton.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import Skeleton"
      pattern: "import.*Skeleton.*from.*ui/skeleton"
---

<objective>
Create UI components for auto-refresh: skeleton loading state and stale data warning banner.

Purpose: Foundation components for Phase 4.1 auto-refresh feature - provides visual feedback during data loading and conflict warnings.
Output: Three reusable components ready for modal integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-auto-refresh-overlay-window/04-CONTEXT.md
@.planning/phases/04.1-auto-refresh-overlay-window/04.1-RESEARCH.md

@src/components/requests/request-detail-modal.tsx
@src/components/ui/dialog.tsx
@src/components/ui/separator.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skeleton component and create modal skeleton layout</name>
  <files>
    src/components/ui/skeleton.tsx
    src/components/loading/request-detail-skeleton.tsx
  </files>
  <action>
1. Install shadcn/ui skeleton component:
   ```bash
   npx shadcn@latest add skeleton
   ```

2. Create `src/components/loading/request-detail-skeleton.tsx` with a skeleton layout that mirrors the request-detail-modal structure:
   - Title skeleton (h-8 w-3/4)
   - Status badge area (flex gap with rounded-full skeletons)
   - Description section (multiple h-4 lines with varying widths)
   - Requester info section (name, email, department skeletons)
   - Action buttons area (h-10 w-32 skeletons)
   - Use Separator components between sections (same as modal)

3. Export RequestDetailSkeleton as named export

The skeleton should match the visual rhythm of the actual modal content so there's minimal layout shift when data loads.
  </action>
  <verify>
- `src/components/ui/skeleton.tsx` exists with Skeleton component
- `src/components/loading/request-detail-skeleton.tsx` exists and exports RequestDetailSkeleton
- TypeScript compiles without errors: `npx tsc --noEmit`
  </verify>
  <done>
- Skeleton base component installed from shadcn/ui
- RequestDetailSkeleton matches modal layout structure
- Components export cleanly for use in modal
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stale data warning banner component</name>
  <files>
    src/components/requests/stale-data-banner.tsx
  </files>
  <action>
Create `src/components/requests/stale-data-banner.tsx`:

```typescript
'use client'

import { AlertTriangle } from 'lucide-react'
import { Button } from '@/components/ui/button'

interface StaleDataBannerProps {
  onRefresh: () => void
}

export function StaleDataBanner({ onRefresh }: StaleDataBannerProps) {
  return (
    <div className="bg-amber-50 border border-amber-200 rounded-lg p-4">
      <div className="flex items-start gap-3">
        <AlertTriangle className="h-5 w-5 text-amber-600 mt-0.5 flex-shrink-0" />
        <div className="flex-1 min-w-0">
          <p className="text-sm font-medium text-amber-900">
            This request was updated by another user
          </p>
          <p className="text-sm text-amber-700 mt-1">
            Click refresh to see the latest changes.
          </p>
        </div>
        <Button onClick={onRefresh} variant="outline" size="sm" className="flex-shrink-0">
          Refresh
        </Button>
      </div>
    </div>
  )
}
```

Key points:
- Amber/yellow color scheme as specified in CONTEXT.md
- AlertTriangle icon from lucide-react (already installed)
- Passive approach - informational banner with manual refresh button
- Responsive layout with proper flex gap and min-width handling
  </action>
  <verify>
- `src/components/requests/stale-data-banner.tsx` exists and exports StaleDataBanner
- TypeScript compiles without errors: `npx tsc --noEmit`
- Component follows shadcn/ui styling patterns
  </verify>
  <done>
- StaleDataBanner component created with amber styling
- Refresh button triggers onRefresh callback
- Component ready for modal integration
  </done>
</task>

</tasks>

<verification>
- All three component files exist and export correctly
- TypeScript compiles: `npx tsc --noEmit`
- No console errors when dev server running
- Skeleton component uses shadcn/ui animation patterns
</verification>

<success_criteria>
- Skeleton component installed from shadcn/ui
- RequestDetailSkeleton matches modal visual structure
- StaleDataBanner provides amber warning with refresh action
- All components are TypeScript-typed and error-free
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-auto-refresh-overlay-window/04.1-01-SUMMARY.md`
</output>
