---
phase: 04.1-auto-refresh-overlay-window
plan: 05
type: execute
wave: 1
depends_on:
  - 04.1-01
  - 04.1-03
files_modified:
  - src/components/loading/request-detail-skeleton.tsx
  - src/components/requests/request-detail-modal.tsx
  - src/app/globals.css
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Content fades in smoothly when data loads after skeleton"
    - "Fade-in animation is 150ms with ease-in timing"
    - "Animation applies to modal content area only (not Dialog shell)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global fadeIn animation keyframes"
      contains: "@keyframes fadeIn, .content-fade-in class"
    - path: "src/components/requests/request-detail-modal.tsx"
      provides: "Content wrapper with fade-in class applied after load"
      contains: "FadeEffect wrapper or conditional className"
    - path: "src/components/loading/request-detail-skeleton.tsx"
      provides: "Skeleton with matching structure for seamless transition"
      contains: "Existing skeleton component (no changes needed)"
  key_links:
    - from: "src/components/requests/request-detail-modal.tsx"
      to: "src/app/globals.css"
      via: "className='content-fade-in' on content wrapper"
      pattern: "content-fade-in"
---

<objective>
Add CSS fade-in animation for smooth content swap after skeleton loading, enhancing visual seamless transition.

Purpose: Address partial gap from verification - skeleton loading minimizes flash but optional CSS fade-in adds extra smoothness. This is NOT blocking but improves perceived performance.

Output: Content fades in over 150ms when data loads, creating seamless transition from skeleton to actual content.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-auto-refresh-overlay-window/04-CONTEXT.md
@.planning/phases/04.1-auto-refresh-overlay-window/04.1-VERIFICATION.md
@.planning/phases/04.1-auto-refresh-overlay-window/04.1-01-SUMMARY.md

# Gap Context (from VERIFICATION.md)

**Partial Gap:** Seamless visual transition - skeleton loading minimizes flash but no verification that transition is completely seamless.

**Current state:** RequestDetailSkeleton mirrors modal structure (7 sections: title, description, requester, actions, solution, approvals, attachments), which minimizes layout shift. However, visual testing required to confirm no perceptible flash.

**Recommendation:** Optional enhancement - add CSS fade-in transition for extra smoothness:
```css
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
.content-fade-in {
  animation: fadeIn 150ms ease-in;
}
```

This is **NOT blocking** since skeleton loading achieves the core goal of better perceived performance.
</context>

<tasks>

<task type="auto">
  <name>Add fade-in animation CSS to global styles</name>
  <files>src/app/globals.css</files>
  <action>
    Add fadeIn keyframe animation and utility class to globals.css:

    1. Locate the globals.css file at src/app/globals.css
    2. Add the following animation keyframes and utility class at the end of the file (after existing styles):

       ```css
       /* Fade-in animation for smooth content transitions */
       @keyframes fadeIn {
         from {
           opacity: 0;
         }
         to {
           opacity: 1;
         }
       }

       .content-fade-in {
         animation: fadeIn 150ms ease-in;
       }
       ```

    3. If globals.css has a section for utilities or animations, add it there instead. Otherwise append to end.

    Notes:
    - 150ms duration provides subtle fade without feeling slow
    - ease-in timing starts slow and accelerates (feels natural for content appearing)
    - Animation runs once and completes (no infinite loop)
  </action>
  <verify>Check that:
    - @keyframes fadeIn exists in globals.css
    - .content-fade-in class exists with animation: fadeIn 150ms ease-in
    - No syntax errors in CSS</verify>
  <done>Global CSS has fadeIn animation and content-fade-in utility class</done>
</task>

<task type="auto">
  <name>Apply fade-in animation to modal content area</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Apply the fade-in animation to the modal content area when data loads:

    1. Add a state to track when content should fade in (after line 65, with other state):
       ```typescript
       const [contentVisible, setContentVisible] = useState(false)
       ```

    2. In the loadRequest() function, set contentVisible to true after data loads successfully:
       - Find the line `setLoading(false)` in the finally block (line 202)
       - Add after it:
       ```typescript
       setContentVisible(true)
       ```

    3. Reset contentVisible when loading starts:
       - Find `setLoading(true)` in loadRequest() (line 94)
       - Add after it:
       ```typescript
       setContentVisible(false)
       ```

    4. Apply the fade-in class to the content area:
       - Find the main content div with className "flex-1 overflow-y-auto px-6 -mx-6" (line 310)
       - Add the conditional className:
       ```typescript
       <div className={`flex-1 overflow-y-auto px-6 -mx-6 ${contentVisible ? 'content-fade-in' : ''}`}>
       ```

       Alternative: If the above causes className issues, wrap the inner content:
       ```typescript
       <div className="flex-1 overflow-y-auto px-6 -mx-6">
         <div className={contentVisible ? 'content-fade-in' : ''}>
           {/* existing content */}
         </div>
       </div>
       ```

    5. Handle the loading state - ensure skeleton doesn't have fade-in:
       - The loading state (lines 259-266) returns early with RequestDetailSkeleton, so no changes needed there

    6. Also handle initial modal open:
       - In the useEffect that calls loadRequest on modal open (line 86-90), the existing logic will set contentVisible through the loadRequest flow
  </action>
  <verify>Check that:
    - contentVisible state is declared
    - setContentVisible(false) is called when loading starts
    - setContentVisible(true) is called when loading completes
    - content-fade-in class is conditionally applied to content area
    - Skeleton loading state does NOT have fade-in class (no flash on initial load)</verify>
  <done>Modal content fades in smoothly after skeleton loading completes</done>
</task>

</tasks>

<verification>
After implementation, verify:

1. **CSS exists**: globals.css has @keyframes fadeIn and .content-fade-in class
2. **State management**: contentVisible state tracks when to apply animation
3. **Animation timing**: contentVisible resets on load start, sets true on load complete
4. **Class application**: content-fade-in class applied conditionally when contentVisible is true
5. **No skeleton flash**: Loading state (skeleton) doesn't have fade-in applied

Visual verification (autonomous - no human checkpoint):
- Fade-in is 150ms (subtle, not distracting)
- Animation applies to content swap after skeleton
- No animation on initial skeleton render

This enhancement is autonomous - no human verification needed as it's a visual polish that can be verified through code inspection and browser testing during execution.
</verification>

<success_criteria>
Success criteria (measurable completion):

1. Code verification:
   - [ ] globals.css has @keyframes fadeIn with opacity 0 to 1
   - [ ] globals.css has .content-fade-in class with 150ms ease-in animation
   - [ ] request-detail-modal.tsx has contentVisible state
   - [ ] setContentVisible(false) called when setLoading(true)
   - [ ] setContentVisible(true) called when setLoading(false)
   - [ ] content-fade-in class applied conditionally to content area

2. Visual behavior:
   - [ ] Content fades in over 150ms when data loads
   - [ ] No fade-in applied to skeleton (prevents flash)
   - [ ] Animation feels smooth and natural
   - [ ] No layout shift during transition (skeleton already handles this)

3. Edge cases:
   - [ ] Repeated refreshes re-trigger animation correctly
   - [ ] No animation artifacts or flickering
   - [ ] Animation respects user prefers-reduced-motion (future enhancement, not required)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-auto-refresh-overlay-window/04.1-05-SUMMARY.md`

Summary should document:
- Fade-in animation added (150ms ease-in)
- Applied to modal content area only
- Skeleton unaffected by animation
- Optional enhancement nature (not blocking for phase completion)
</output>
