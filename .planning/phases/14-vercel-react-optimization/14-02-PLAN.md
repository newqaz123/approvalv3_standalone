---
phase: 14-vercel-react-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/(dashboard)/requests/[requestId]/page.tsx", "src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx"]
autonomous: true

must_haves:
  truths:
    - "Request detail pages fetch data in parallel (no sequential awaits)"
    - "Independent queries execute concurrently"
    - "Request detail load time reduced"
  artifacts:
    - path: "src/app/(dashboard)/requests/[requestId]/page.tsx"
      provides: "Request detail page with parallel data fetching"
      exports: ["default"]
    - path: "src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx"
      provides: "Engineering solution page with parallel data fetching"
      exports: ["default"]
  key_links:
    - from: "src/app/(dashboard)/requests/[requestId]/page.tsx"
      to: "server-actions"
      via: "Parallel data fetching"
      pattern: "Promise\\.all"
    - from: "src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx"
      to: "server-actions"
      via: "Parallel data fetching"
      pattern: "Promise\\.all"
---

<objective>
Eliminate waterfalls in request detail pages by parallelizing independent data fetches.

**Purpose:** Request detail pages currently fetch request, approvals, and activity data sequentially. Parallelizing independent queries reduces load time.

**Output:** Request detail pages with all independent data fetched in parallel using Promise.all().
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vercel-react-optimization/14-RESEARCH.md

@src/app/(dashboard)/requests/[requestId]/page.tsx
@src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Refactor request detail page to parallel data fetching</name>
  <files>src/app/(dashboard)/requests/[requestId]/page.tsx</files>
  <action>
  1. Read current implementation
  2. Identify all sequential await calls for data fetching
  3. Determine which queries are independent (no dependencies on each other's results)
  4. Refactor independent queries to use Promise.all():

  ```typescript
  // Before: Sequential
  const request = await getRequest(requestId)
  const approvals = await getRequestApprovals(requestId)
  const activity = await getRequestActivity(requestId)

  // After: Parallel
  const [request, approvals, activity] = await Promise.all([
    getRequest(requestId),
    getRequestApprovals(requestId),
    getRequestActivity(requestId)
  ])
  ```

  5. Keep dependent queries (if any) outside Promise.all()
  6. Add comment explaining parallelization pattern

  Reference: `.agents/skills/vercel-react-best-practices/rules/async-parallel.md`
  </action>
  <verify>
  grep -n "Promise.all" src/app/\(dashboard\)/requests/\[requestId\]/page.tsx
  </verify>
  <done>
  Request detail page uses Promise.all() for all independent data fetches. Sequential awaits eliminated.
  </done>
</task>

<task type="auto">
  <name>Refactor engineering solution page to parallel data fetching</name>
  <files>src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx</files>
  <action>
  1. Read current implementation
  2. Identify all sequential await calls for data fetching
  3. Determine which queries are independent
  4. Refactor independent queries to use Promise.all()
  5. Keep dependent queries sequential (if any)
  6. Add comment explaining parallelization pattern

  Apply same Promise.all() pattern as request detail page.
  </action>
  <verify>
  grep -n "Promise.all" src/app/\(dashboard\)/engineering/solutions/\[requestId\]/page.tsx
  </verify>
  <done>
  Engineering solution page uses Promise.all() for all independent data fetches. Sequential awaits eliminated.
  </done>
</task>

</tasks>

<verification>
- [ ] Promise.all() used in both detail pages
- [ ] All independent queries execute in parallel
- [ ] Dependent queries (if any) remain sequential after parallel block
- [ ] No TypeScript errors
- [ ] Pages render correctly with all data
</verification>

<success_criteria>
1. Request detail page load time reduced by 40%+ (measured via DevTools)
2. Engineering solution page load time reduced by 40%+
3. All data displays correctly on both pages
4. No console errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/14-vercel-react-optimization/14-02-SUMMARY.md`
</output>
