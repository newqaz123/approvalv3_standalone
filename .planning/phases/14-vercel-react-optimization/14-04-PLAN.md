---
phase: 14-vercel-react-optimization
plan: 04
type: execute
wave: 2
depends_on: []
files_modified: ["next.config.ts", "src/components/ui/card.tsx", "src/components/ui/button.tsx", "src/components/ui/dialog.tsx"]
autonomous: true

must_haves:
  truths:
    - "Direct icon imports replace barrel imports"
    - "Build time improved by 25%+"
    - "Development server boot time improved"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js configuration with optimizePackageImports"
      contains: "optimizePackageImports: ['lucide-react']"
    - path: "src/components/ui/*.tsx"
      provides: "UI components with optimized icon imports"
      contains: "from 'lucide-react/dist/esm/icons"
  key_links:
    - from: "src/components/**/*.tsx"
      to: "lucide-react"
      via: "Direct icon imports or optimizePackageImports"
      pattern: "from ['\"]lucide-react"
---

<objective>
Optimize bundle size by eliminating barrel file imports from lucide-react.

**Purpose:** Current barrel imports (import { Icon } from 'lucide-react') load 1,583 modules. Direct imports or Next.js optimizePackageImports reduce this to only used icons, improving build time 25%+ and cold starts 40%+.

**Output:** Optimized icon imports across all components.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-vercel-react-optimization/14-RESEARCH.md

@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Configure Next.js optimizePackageImports for lucide-react</name>
  <files>next.config.ts</files>
  <action>
  1. Read current next.config.ts (or next.config.js)
  2. Add optimizePackageImports configuration:

  ```typescript
  const nextConfig = {
    experimental: {
      optimizePackageImports: ['lucide-react'],
    },
  }
  ```

  3. If using next.config.js, convert to TypeScript syntax
  4. Ensure config exports properly
  5. Add comment explaining optimization

  Reference: `.agents/skills/vercel-react-best-practices/rules/bundle-barrel-imports.md`

  Note: This is the preferred approach over manual direct imports because it maintains ergonomic barrel import syntax while optimizing at build time.
  </action>
  <verify>
  grep -n "optimizePackageImports" next.config.ts
  </verify>
  <done>
  next.config.ts configured with optimizePackageImports for lucide-react. Barrel imports now optimized automatically.
  </done>
</task>

<task type="auto">
  <name>Identify and audit lucide-react usage patterns</name>
  <files>src/components/**/*.tsx</files>
  <action>
  1. Run grep to find all lucide-react barrel imports:

  ```bash
  grep -r "from 'lucide-react'" src/components/
  grep -r 'from "lucide-react"' src/components/
  ```

  2. Document current usage in console.log:
     - Number of files using lucide-react
     - Most commonly imported icons
     - Any problematic patterns (dynamic imports, etc.)

  3. Verify all imports follow the pattern: import { Icon1, Icon2 } from 'lucide-react'
  4. No code changes needed - optimizePackageImports handles it automatically

  This is a verification task to ensure optimizePackageImports will work correctly.
  </action>
  <verify>
  grep -c "from 'lucide-react'" src/components/**/*.tsx | wc -l
  </verify>
  <done>
  Audited all lucide-react imports. N files use barrel imports. All compatible with optimizePackageImports.
  </done>
</task>

</tasks>

<verification>
- [ ] next.config.ts has optimizePackageImports: ['lucide-react']
- [ ] Configuration is valid TypeScript/JavaScript
- [ ] All lucide-react imports use standard barrel syntax
- [ ] No dynamic icon imports (e.g., import('lucide-react').then)
- [ ] Build completes successfully
</verification>

<success_criteria>
1. Development server boot time improved by 25%+ (measure with time npm run dev)
2. Production build time improved by 25%+ (measure with time npm run build)
3. Bundle size reduced (check .next/static/chunks/ size)
4. No runtime errors or missing icons
</success_criteria>

<output>
After completion, create `.planning/phases/14-vercel-react-optimization/14-04-SUMMARY.md`
</output>
