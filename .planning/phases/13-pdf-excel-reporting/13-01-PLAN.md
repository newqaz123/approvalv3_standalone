---
phase: 13-pdf-excel-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - Dockerfile
  - .env.example
  - src/lib/pdf.ts
autonomous: true

must_haves:
  truths:
    - "PDF generation completes successfully when given valid request data"
    - "Chromium browser runs in Docker environment for headless PDF generation"
    - "HTML template renders all request sections (header, description, solution, approvals, activities)"
    - "Generated PDF includes A4 formatting with proper margins and print-friendly styling"
    - "PDF buffer can be returned from server action for client download"
  artifacts:
    - path: "package.json"
      contains: "puppeteer"
    - path: "Dockerfile"
      contains: "chromium"
    - path: "src/lib/pdf.ts"
      provides: "PDF generation utilities with HTML template"
      min_lines: 50
      exports: ["generateRequestPDF", "RequestPDFData"]
  key_links:
    - from: "src/lib/pdf.ts"
      to: "puppeteer"
      via: "import statement"
      pattern: "import puppeteer from 'puppeteer'"
---

<objective>
Install and configure Puppeteer for PDF generation with Docker support.

Purpose: Set up the foundation for server-side PDF generation using Puppeteer's headless Chrome.
Output: Working PDF generation library with Docker-compatible Chromium configuration.
</objective>

<execution_context>
@/Users/red-copperpot/.claude/get-shit-done/workflows/execute-plan.md
@/Users/red-copperpot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-pdf-excel-reporting/13-CONTEXT.md
@.planning/phases/13-pdf-excel-reporting/13-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/Users/red-copperpot/Documents/MyProjects/ApprovalAppV2/Dockerfile
@/Users/red-copperpot/Documents/MyProjects/ApprovalAppV2/package.json
</context>

<tasks>

<task type="auto">
  <name>Install Puppeteer and configure environment</name>
  <files>
    package.json
    .env.example
  </files>
  <action>
    1. Run: npm install puppeteer@^23.0.0 --save
    2. Add to .env.example: PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
    3. Add to .env.example: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

    Do NOT modify .env.local (only update the example file).
  </action>
  <verify>
    grep -q "puppeteer" package.json && grep -q "PUPPETEER" .env.example
  </verify>
  <done>
    Puppeteer is installed and environment variables are documented in .env.example
  </done>
</task>

<task type="auto">
  <name>Add Chromium to Dockerfile for Puppeteer</name>
  <files>
    Dockerfile
  </files>
  <action>
    Modify the Dockerfile base stage to include Chromium and required fonts:

    In the `FROM node:20-alpine AS base` section, update the RUN apk add command to include:
    - chromium
    - nss
    - freetype
    - harfbuzz
    - ca-certificates
    - ttf-freefont

    After the apk add line, add environment variables:
    ENV PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
    ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

    See research documentation for exact Dockerfile pattern.
  </action>
  <verify>
    grep -q "chromium" Dockerfile && grep -q "PUPPETEER_EXECUTABLE_PATH" Dockerfile
  </verify>
  <done>
    Dockerfile includes Chromium and Puppeteer environment variables for headless operation
  </done>
</task>

<task type="auto">
  <name>Create PDF generation library with HTML template</name>
  <files>
    src/lib/pdf.ts
  </files>
  <action>
    Create src/lib/pdf.ts with:

    1. Import puppeteer
    2. Define RequestPDFData interface matching the research spec (title, description, requester, department, status, createdAt, completedAt, solution, fileAttachments, approvals, activities, generatedBy)
    3. Export generateRequestPDF(data: RequestPDFData): Promise<Buffer> function that:
       - Launches Puppeteer with Docker-compatible args (--no-sandbox, --disable-setuid-sandbox, --disable-dev-shm-usage, --disable-gpu)
       - Sets executablePath from process.env.PUPPETEER_EXECUTABLE_PATH
       - Renders HTML from template
       - Generates A4 PDF with margins (top: 20mm, right: 15mm, bottom: 15mm, left: 15mm)
       - Returns Buffer from PDF
    4. Internal renderRequestHTML function with:
       - Inline CSS for print-friendly styling
       - Helper functions: formatDate, formatFileSize
       - Header with title and "Approval Request Report"
       - Request information section (requester, department, email, status, created, completed dates)
       - Description section with pre-wrap
       - Engineering solution section (if exists): title, cost estimate, timeline, submitted by, concept design, file attachments
       - Request attachments section (file list with metadata)
       - Approval history timeline with colored dots (green=approved, red=rejected, gray=pending)
       - Activity timeline section
       - Footer with "Generated on DATE by USER from Approval System"

    Use the complete HTML template from the research documentation. Ensure all data fields are properly escaped to prevent HTML injection.
  </action>
  <verify>
    test -f src/lib/pdf.ts && grep -q "generateRequestPDF" src/lib/pdf.ts && grep -q "RequestPDFData" src/lib/pdf.ts
  </verify>
  <done>
    PDF generation library exists with complete HTML template for request documentation
  </done>
</task>

</tasks>

<verification>
1. Check package.json contains puppeteer dependency
2. Check Dockerfile includes chromium and Puppeteer environment variables
3. Check src/lib/pdf.ts exports generateRequestPDF and RequestPDFData interface
4. Verify HTML template includes all required sections (header, request info, description, solution, attachments, approval history, activities, footer)
</verification>

<success_criteria>
Puppeteer is installed and configured for Docker environment. PDF generation utility is ready to be called from Server Actions with complete HTML template for rendering request documentation.
</success_criteria>

<output>
After completion, create `.planning/phases/13-pdf-excel-reporting/13-01-SUMMARY.md`
</output>
