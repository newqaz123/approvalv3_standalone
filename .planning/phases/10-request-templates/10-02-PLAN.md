---
phase: 10-request-templates
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/(dashboard)/requests/new/page.tsx
  - src/components/requests/request-form.tsx
autonomous: true
must_haves:
  truths:
    - "User sees a template selector dropdown on the 'New Request' page"
    - "Selecting a template automatically fills Title and Description"
    - "Selecting 'Blank Request' clears Title and Description"
    - "Default template (if exists) is selected by default on load"
  artifacts:
    - path: "src/components/requests/request-form.tsx"
      provides: "Template selection UI"
      contains: "Select onValueChange"
    - path: "src/app/(dashboard)/requests/new/page.tsx"
      provides: "Template data fetching"
      contains: "getTemplates"
  key_links:
    - from: "src/app/(dashboard)/requests/new/page.tsx"
      to: "src/components/requests/request-form.tsx"
      via: "props"
      pattern: "<RequestForm.*templates="
---

<objective>
Integrate templates into the User Request creation workflow.
Purpose: Allow users to quickly fill request forms using predefined templates.
Output: Updated New Request page with template selection functionality.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-request-templates/10-01-SUMMARY.md
@src/components/requests/request-form.tsx
@src/app/(dashboard)/requests/new/page.tsx

**Pattern:** Fetch data in Server Component (`new/page.tsx`), pass to Client Component (`request-form.tsx`).
**UX:** Template selection overwrites current fields (user should be aware or it should be obvious).
**Default:** If a default template exists, the form should initialize with it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch Templates in Server Component</name>
  <files>src/app/(dashboard)/requests/new/page.tsx</files>
  <action>
    1. Import `getTemplates` from `@/server-actions/templates`.
    2. Fetch active templates: `await getTemplates({ activeOnly: true })`.
    3. Pass `templates` array to `RequestForm` component as a prop.
    4. Pass `defaultTemplateId` (if any) as a prop.
  </action>
  <verify>
    - Add console.log in page to verify data fetch.
    - Check props passing logic.
  </verify>
  <done>
    - `NewRequestPage` passes templates to `RequestForm`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Template Selector in RequestForm</name>
  <files>src/components/requests/request-form.tsx</files>
  <action>
    1. Update `RequestFormProps` to include `templates: Template[]`.
    2. Add `defaultTemplateId` prop.
    3. Add a `<Select>` component (Shadcn UI) above the Title field.
    4. Implement selection logic:
       - On select: Find template, `form.setValue('title', t.title, { shouldDirty: true })`, `form.setValue('description', t.description, { shouldDirty: true })`.
       - On select "blank": Set values to empty string.
    5. Handle default value:
       - In `useForm` defaultValues, use the default template's values if provided.
       - Pre-select the dropdown value.
  </action>
  <verify>
    - Visit `/requests/new`.
    - Verify dropdown lists templates.
    - Select a template -> fields populate.
    - Select "Blank" -> fields clear.
    - Reload -> Default template loads automatically.
  </verify>
  <done>
    - RequestForm supports template selection and auto-filling.
  </done>
</task>

</tasks>

<verification>
- Create a request using a template
- Verify the title and description match the template
- Create a request using "Blank" option
- Verify no template content persists
</verification>

<success_criteria>
- Users can choose from active templates
- Form pre-fills correctly
- Default template logic works
</success_criteria>

<output>
After completion, create .planning/phases/10-request-templates/10-02-SUMMARY.md
</output>
