---
phase: 10-request-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/server-actions/templates.ts
  - src/app/admin/templates/page.tsx
  - src/app/admin/templates/new/page.tsx
  - src/app/admin/templates/[id]/page.tsx
  - src/app/(admin)/admin/page.tsx
autonomous: true
must_haves:
  truths:
    - "Admin can see a 'Request Templates' option in the admin dashboard"
    - "Admin can create a new template with title and description"
    - "Admin can toggle a template as 'Default' (only one active at a time)"
    - "Admin can edit existing templates"
    - "Admin can soft-delete templates"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Template model"
      contains: "model Template"
    - path: "src/server-actions/templates.ts"
      provides: "CRUD operations"
      exports: ["createTemplate", "updateTemplate", "getTemplates", "setTemplateDefault"]
    - path: "src/app/admin/templates/page.tsx"
      provides: "Template management UI"
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "/admin/templates"
      via: "Link component"
    - from: "src/server-actions/templates.ts"
      to: "prisma.template"
      via: "prisma.$transaction"
      pattern: "prisma\\.\\$transaction"
---

<objective>
Implement the backend data model and admin interface for Request Templates.
Purpose: Allow administrators to manage standardized request templates (Create, Read, Update, Delete, Set Default).
Output: A fully functional admin section for templates and the underlying database schema.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@prisma/schema.prisma
@src/app/(admin)/admin/page.tsx

**Decision:** Templates are global (no department relation).
**Decision:** Only one template can be default at a time (use transaction).
**Decision:** Templates have internal name, predefined title, and description content.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Template Model and Migration</name>
  <files>prisma/schema.prisma</files>
  <action>
    1. Add `Template` model to schema:
       - id (cuid)
       - name (String, internal admin name)
       - title (String, predefined title)
       - description (String, Text)
       - isDefault (Boolean, default false)
       - isActive (Boolean, default true)
       - Timestamps
    2. Run migration `npx prisma migrate dev --name add_templates`.
  </action>
  <verify>
    - `npx prisma validate` passes.
    - `npx prisma studio` shows the new Template table.
  </verify>
  <done>
    - Schema updated and migration applied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Server Actions</name>
  <files>src/server-actions/templates.ts</files>
  <action>
    Create server actions with Zod validation:
    - `createTemplate(data)`: Create new template.
    - `updateTemplate(id, data)`: Update existing.
    - `toggleTemplateStatus(id)`: Toggle isActive.
    - `setTemplateDefault(id)`: Use `prisma.$transaction` to set `isDefault: false` for all, then `isDefault: true` for target.
    - `getTemplates(options)`: Fetch templates (support filter by active/all).
    - `getTemplate(id)`: Fetch single template.
    Use `revalidatePath('/admin/templates')` after mutations.
  </action>
  <verify>
    - Create a test script or use `npm run dev` to invoke actions (after UI is built).
    - Check transaction logic ensures only one default.
  </verify>
  <done>
    - Server actions file created with all CRUD methods.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Admin Dashboard UI</name>
  <files>
    src/app/admin/templates/page.tsx
    src/app/admin/templates/new/page.tsx
    src/app/admin/templates/[id]/page.tsx
    src/app/(admin)/admin/page.tsx
  </files>
  <action>
    1. Create Template List (`/admin/templates/page.tsx`):
       - Table showing Name, Title, Default status, Active status.
       - Actions: Edit, Toggle Active, Set Default.
       - "Create New" button.
    2. Create Editor (`new/page.tsx`, `[id]/page.tsx`):
       - Form with Name, Title, Description.
       - Use `react-hook-form` + `zod`.
    3. Update Dashboard (`src/app/(admin)/admin/page.tsx`):
       - Add "Request Templates" card linking to `/admin/templates`.
       - Use `LayoutTemplate` icon from lucide-react.
  </action>
  <verify>
    - Visit `/admin` -> Click "Request Templates".
    - Create a template -> verify it appears in list.
    - Set as default -> verify badge updates.
    - Edit template -> verify changes persist.
  </verify>
  <done>
    - Admin UI complete and accessible.
  </done>
</task>

</tasks>

<verification>
- Navigate to /admin, click "Request Templates"
- Create 2 templates
- Mark one as default, verify the other unmarks
- Deactivate one, verify it shows as inactive in list
</verification>

<success_criteria>
- Template model exists in DB
- Admin can perform full CRUD on templates
- "Set Default" logic guarantees at most one default template
</success_criteria>

<output>
After completion, create .planning/phases/10-request-templates/10-01-SUMMARY.md
</output>
