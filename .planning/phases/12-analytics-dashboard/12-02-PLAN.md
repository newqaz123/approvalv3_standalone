---
phase: 12-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/analytics/loading.tsx
  - src/components/analytics/analytics-page.tsx
  - src/components/analytics/summary-cards.tsx
autonomous: true

must_haves:
  truths:
    - User can access analytics page at /analytics
    - Analytics page shows summary cards with key metrics (total requests, pending, avg time, approval rate)
    - Summary cards display correctly even when no data exists
    - Analytics page loads with default 30-day filter
  artifacts:
    - path: src/app/(dashboard)/analytics/page.tsx
      provides: Server Component for analytics page
      contains: async AnalyticsDashboard function
    - path: src/app/(dashboard)/analytics/loading.tsx
      provides: Loading skeleton for analytics page
      contains: Skeleton components for summary metrics
    - path: src/components/analytics/analytics-page.tsx
      provides: Client Component wrapper for analytics
      contains: AnalyticsPage function with 'use client' directive
    - path: src/components/analytics/summary-cards.tsx
      provides: Summary metric cards component
      contains: SummaryCards function
  key_links:
    - from: src/app/(dashboard)/analytics/page.tsx
      to: src/server-actions/analytics.ts
      via: getAnalyticsData call
      pattern: getAnalyticsData\(
    - from: src/components/analytics/analytics-page.tsx
      to: src/server-actions/analytics.ts
      via: getAnalyticsData call on filter change
      pattern: getAnalyticsData\(newFilters\)
    - from: src/components/analytics/analytics-page.tsx
      to: src/components/analytics/summary-cards.tsx
      via: Component usage
      pattern: <SummaryCards
---

<objective>
Create the analytics page structure including the Server Component route, loading skeleton, client component wrapper, and summary cards displaying key metrics (total requests, pending approvals, average approval time, approval rate).

Purpose: Establish the page structure and visual foundation for the analytics dashboard, enabling users to view high-level metrics about approval workflow performance.
Output: Analytics page route, loading skeleton, client component wrapper, and summary cards displaying key metrics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-analytics-dashboard/12-analytics-CONTEXT.md
@.planning/phases/12-analytics-dashboard/12-RESEARCH.md
@.planning/phases/12-analytics-dashboard/12-01-SUMMARY.md
@src/server-actions/analytics.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics page route (Server Component)</name>
  <files>src/app/(dashboard)/analytics/page.tsx</files>
  <action>Create src/app/(dashboard)/analytics/page.tsx as a Server Component:

1. Import auth from @clerk/nextjs/server, AnalyticsPage client component, getAnalyticsData, getAnalyticsFilters
2. Check userId from auth()
3. Fetch initial data with default filter: {dateRange: '30days', departmentId: undefined, status: undefined, requesterId: undefined}
4. Fetch filters (departments, users) for filter controls
5. Return <AnalyticsPage initialData={initialData} filters={filters} userId={userId} />

Page must handle unauthenticated users by redirecting or showing unauthorized message.
</action>
  <verify>Visit /analytics while authenticated - page renders without errors. Server Component fetches initial data and passes to client component.</verify>
  <done>Analytics page route created with Server Component fetching initial data</done>
</task>

<task type="auto">
  <name>Task 2: Create loading skeleton</name>
  <files>src/app/(dashboard)/analytics/loading.tsx</files>
  <action>Create src/app/(dashboard)/analytics/loading.tsx with skeleton loading state:

1. Use existing Skeleton components from src/components/ui/skeleton.tsx
2. Display 4 skeleton cards for summary metrics
3. Use standard skeleton pattern with animate-pulse
4. Match approximate layout of actual analytics page

Loading skeleton must provide visual feedback while Server Component fetches data.
</action>
  <verify>Visit /analytics - loading skeleton appears briefly before data loads</verify>
  <done>Loading skeleton created for analytics page</done>
</task>

<task type="auto">
  <name>Task 3: Create AnalyticsPage client component</name>
  <files>src/components/analytics/analytics-page.tsx</files>
  <action>Create src/components/analytics/analytics-page.tsx as a Client Component:

1. Mark with 'use client' directive
2. Accept props: initialData (AnalyticsData), filters (AnalyticsFilters), userId (string | null)
3. State: data (useState initialized with initialData), isLoading (useState false)
4. handleFilterChange function: Set isLoading true, call getAnalyticsData(newFilters), update data state, set isLoading false
5. Render div container with:
   - Conditional rendering: if isLoading && !data, show loading message (Skeleton handled by page.tsx)
   - If data exists, render SummaryCards component with data.summary
   - Placeholder comments for chart components (to be added in plans 12-03 and 12-04)

Component must handle userId check - redirect if not authenticated.
</action>
  <verify>Component renders without TypeScript errors. handleFilterChange function calls getAnalyticsData and updates state.</verify>
  <done>Client component created with state management for analytics data and filter changes</done>
</task>

<task type="auto">
  <name>Task 4: Create SummaryCards component</name>
  <files>src/components/analytics/summary-cards.tsx</files>
  <action>Create src/components/analytics/summary-cards.tsx with metric cards:

1. Accept summary: SummaryMetrics prop
2. Render Grid container with grid-cols-2 md:grid-cols-4 gap-4
3. Create 4 metric cards:
   - Total Requests: Display summary.totalRequests with icon (ClipboardList)
   - Pending: Display summary.pendingRequests with icon (Clock)
   - Avg Approval Time: Display summary.avgApprovalTime.toFixed(1) + " days" with icon (Timer)
   - Approval Rate: Display summary.approvalRate.toFixed(1) + "%" with icon (CheckCircle)
4. Each card uses Card component from src/components/ui/card.tsx with CardHeader and CardContent
5. Add subtle trend indicators (up/down arrows) if available (optional enhancement)

Cards must handle empty data gracefully - show "—" for undefined values.
</action>
  <verify>Summary cards render with 4 metrics showing correct values from summary prop. Empty data handled without errors.</verify>
  <done>Summary cards component created displaying key metrics</done>
</task>

</tasks>

<verification>
- [ ] Analytics page accessible at /analytics route
- [ ] Summary cards display total requests, pending, avg time, approval rate
- [ ] Server Actions return valid data structure for default 30-day filter
- [ ] Loading skeleton appears briefly on page load
- [ ] No TypeScript errors in new files
- [ ] Analytics page works on mobile screens (check responsive classes from Phase 11)
</verification>

<success_criteria>
1. Analytics page is accessible at /analytics route
2. Summary cards display 4 key metrics (total requests, pending, avg time, approval rate)
3. Default filter (30 days) is applied on initial page load
4. Loading skeleton shows while Server Component fetches data
5. All type definitions match actual data structures returned by Server Actions
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-dashboard/12-02-SUMMARY.md`
</output>
