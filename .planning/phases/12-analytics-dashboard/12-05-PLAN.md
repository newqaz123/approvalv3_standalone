---
phase: 12-analytics-dashboard
plan: 05
type: execute
wave: 4
depends_on: [12-03, 12-04]
files_modified:
  - src/components/analytics/filter-controls.tsx
  - src/components/analytics/analytics-page.tsx
autonomous: true

must_haves:
  truths:
    - User can filter analytics by date range (7/30/90 days, All time)
    - User can filter analytics by department
    - User can filter analytics by status
    - User can filter analytics by requester
    - Changing filters refreshes all charts with filtered data
    - Filters reset to default (30 days, all departments) on page load
    - Filter state is persisted in URL search params (shareable links)
  artifacts:
    - path: src/components/analytics/filter-controls.tsx
      provides: Global filter controls component
      contains: FilterControls function with Select and date range inputs
    - path: src/components/analytics/analytics-page.tsx
      provides: Analytics page with integrated filters
      contains: FilterControls usage and URL search params state management
  key_links:
    - from: src/components/analytics/filter-controls.tsx
      to: src/components/analytics/analytics-page.tsx
      via: Component usage with onFilterChange callback
      pattern: <FilterControls.*onFilterChange
    - from: src/components/analytics/analytics-page.tsx
      to: src/server-actions/analytics.ts
      via: getAnalyticsData call on filter change
      pattern: getAnalyticsData\(newFilters\)
    - from: src/components/analytics/filter-controls.tsx
      to: src/types/analytics.ts
      via: AnalyticsFilters type
      pattern: import.*AnalyticsFilters
    - from: src/components/analytics/analytics-page.tsx
      to: next/navigation
      via: useSearchParams and useRouter
      pattern: useSearchParams|useRouter
---

<objective>
Create filter controls component with date range presets (7/30/90 days, All time), department dropdown, status dropdown, and requester dropdown. Integrate filters into analytics page using URL search params for state management, enabling shareable filtered URLs and persistent state across page reloads.

Purpose: Enable users to explore analytics data by time range, department, status, and requester, providing flexibility to analyze specific subsets of data with persistent, shareable filter states.
Output: Filter controls component integrated into analytics page with URL-based state management for filters.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-analytics-dashboard/12-analytics-CONTEXT.md
@.planning/phases/12-analytics-dashboard/12-RESEARCH.md
@.planning/phases/12-analytics-dashboard/12-01-SUMMARY.md
@.planning/phases/12-analytics-dashboard/12-02-SUMMARY.md
@.planning/phases/12-analytics-dashboard/12-03-SUMMARY.md
@.planning/phases/12-analytics-dashboard/12-04-SUMMARY.md
@src/types/analytics.ts
@src/components/ui/select.tsx
@src/components/ui/popover.tsx
@src/components/ui/calendar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FilterControls component</name>
  <files>src/components/analytics/filter-controls.tsx</files>
  <action>Create src/components/analytics/filter-controls.tsx with global filter controls:

1. Mark with 'use client' directive
2. Import from next/navigation: useSearchParams
3. Accept props: filters (AnalyticsFilters), departments (Array<{id: string, name: string}>), users (Array<{id: string, name: string}>), onFilterChange callback
4. Use useSearchParams() to sync filter values with URL:
   - Read initial values from URL params: searchParams.get('dateRange'), searchParams.get('departmentId'), etc.
   - Default to '30days' for dateRange if not present
5. Create filter controls in a responsive grid layout (Grid with cols-1 md:cols-2 lg:cols-4, gap-4):

   **Date Range Filter:**
   - Use Button or Select for presets: 7 days, 30 days, 90 days, All time
   - Store state locally (useState) synced with URL params
   - Use date-fns for calculations (already implemented in Server Actions)
   - Default: '30days'

   **Department Filter:**
   - Use Select component from @radix-ui/react-select (src/components/ui/select.tsx)
   - Options: "All Departments" + department names from filters.departments
   - Value: departmentId string or undefined
   - Default: undefined (all departments)

   **Status Filter:**
   - Use Select component
   - Options: "All Statuses" + status values (Pending, Approved, Rejected, Completed, etc.)
   - Value: status string or undefined
   - Default: undefined (all statuses)

   **Requester Filter:**
   - Use Select component
   - Options: "All Requesters" + user names from filters.users
   - Value: requesterId string or undefined
   - Default: undefined (all requesters)

6. Implement handleApplyFilter function:
   - Construct newFilters object with current values
   - Call onFilterChange(newFilters)
   - Parent component updates URL via router.push()

7. Render all filter controls in Card with CardHeader "Filters"

Each filter must have clear labels and placeholder text indicating "All [X]" for default state.
</action>
  <verify>Filter controls render with 4 filters. Select dropdowns show correct options. handleApplyFilter calls onFilterChange with correct filter object. URL params update when filters change.</verify>
  <done>Filter controls component created with date range, department, status, and requester filters</done>
</task>

<task type="auto">
  <name>Task 2: Integrate FilterControls with URL search params</name>
  <files>src/components/analytics/analytics-page.tsx</files>
  <action>Update src/components/analytics/analytics-page.tsx to integrate filter controls with URL state management:

1. Import from next/navigation: useSearchParams, useRouter
2. Import FilterControls component
3. Replace component state (useState) with URL search params:
   - const searchParams = useSearchParams()
   - const router = useRouter()
   - Parse filters from URL params on mount:
     * dateRange: searchParams.get('dateRange') || '30days'
     * departmentId: searchParams.get('departmentId') || undefined
     * status: searchParams.get('status') || undefined
     * requesterId: searchParams.get('requesterId') || undefined
4. Update handleFilterChange function:
   - Set isLoading true
   - Call getAnalyticsData(newFilters)
   - Update data state with new data
   - Set isLoading false
   - Update URL: router.push(`/analytics?${new URLSearchParams(newFilters).toString()}`)
5. Render FilterControls at the TOP of the page (before SummaryCards):
   - Pass: filters={filters} departments={filters.departments} users={filters.users} onFilterChange={handleFilterChange}
6. Layout order: FilterControls → SummaryCards → Pipeline Chart → Department Breakdown → Time Metrics

Filter controls must be at the top so users can filter before viewing charts. URL params provide persistent, shareable filter state.
</action>
  <verify>Filter controls appear at top of analytics page. Changing any filter updates URL with new search params. All charts update with filtered data. URL can be bookmarked and shares the filtered view.</verify>
  <done>Filter controls integrated with URL search params for persistent, shareable state</done>
</task>

<task type="auto">
  <name>Task 3: Add loading state during filter changes</name>
  <files>src/components/analytics/analytics-page.tsx</files>
  <action>Enhance src/components/analytics/analytics-page.tsx with loading state during filter changes:

1. If isLoading is true and data exists (not initial load), show loading indicator:
   - Use Skeleton components for summary cards
   - Show "Updating charts..." message or spinner
   - Keep existing charts visible but dimmed or with loading overlay
2. Initial page load loading is handled by loading.tsx (already created in plan 12-02)
3. Filter change loading should be fast and provide visual feedback
4. URL params update immediately (no need to wait for data fetch)

Loading state must clearly indicate that data is being refreshed due to filter changes.
</action>
  <verify>Change a filter value - URL updates immediately, loading state appears briefly, then charts update with new data. Initial page load shows loading.tsx skeleton.</verify>
  <done>Loading state implemented for filter changes with visual feedback</done>
</task>

</tasks>

<verification>
- [ ] Filter controls appear at top of analytics page
- [ ] Date range filter has presets: 7 days, 30 days, 90 days, All time
- [ ] Department filter shows all departments with "All Departments" default
- [ ] Status filter shows all statuses with "All Statuses" default
- [ ] Requester filter shows all users with "All Requesters" default
- [ ] Changing any filter updates URL search params immediately
- [ ] Changing any filter refreshes all charts with filtered data
- [ ] Loading state appears during filter changes
- [ ] Filters reset to default (30 days, all departments) on page load with no params
- [ ] Filter controls are responsive on mobile screens (single column on mobile)
- [ ] Filtered URLs are shareable (bookmarking preserves filters)
</verification>

<success_criteria>
1. Filter controls integrated at top of analytics page with 4 filters (date, department, status, requester)
2. Changing any filter updates URL search params immediately and refreshes all charts with filtered data
3. Loading state provides visual feedback during data refresh
4. Filters work on mobile screens with responsive layout
5. Default filter state is 30 days, all departments, all statuses, all requesters
6. Filter state is persistent across page reloads via URL params
7. Filtered views are shareable via URLs
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-dashboard/12-05-SUMMARY.md`
</output>
