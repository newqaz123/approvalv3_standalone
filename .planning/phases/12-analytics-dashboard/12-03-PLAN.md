---
phase: 12-analytics-dashboard
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - src/components/analytics/chart-utils.ts
  - src/components/analytics/workflow-pipeline-chart.tsx
  - src/components/analytics/department-breakdown-chart.tsx
  - src/components/analytics/analytics-page.tsx
autonomous: true

must_haves:
  truths:
    - User can see workflow pipeline as stacked bar chart with status breakdown
    - Workflow pipeline chart shows each status with green (approved), yellow (pending), red (rejected) colors
    - User can see department breakdown as pie/horizontal bar chart
    - Charts are responsive and work on mobile screens
    - Chart tooltips show detailed values on hover
  artifacts:
    - path: src/components/analytics/chart-utils.ts
      provides: Shared chart utilities and color constants
      contains: STATUS_COLORS, DEPT_COLORS constants
    - path: src/components/analytics/workflow-pipeline-chart.tsx
      provides: Stacked bar chart for workflow pipeline
      contains: WorkflowPipelineChart function with BarChart from recharts
    - path: src/components/analytics/department-breakdown-chart.tsx
      provides: Pie/horizontal bar chart for department breakdown
      contains: DepartmentBreakdownChart function with PieChart or BarChart from recharts
  key_links:
    - from: src/components/analytics/workflow-pipeline-chart.tsx
      to: src/types/analytics.ts
      via: WorkflowPipelineSegment type import
      pattern: import.*WorkflowPipelineSegment
    - from: src/components/analytics/analytics-page.tsx
      to: src/components/analytics/workflow-pipeline-chart.tsx
      via: Component usage
      pattern: <WorkflowPipelineChart
    - from: src/components/analytics/analytics-page.tsx
      to: src/components/analytics/department-breakdown-chart.tsx
      via: Component usage
      pattern: <DepartmentBreakdownChart
---

<objective>
Create shared chart utilities with color constants, build workflow pipeline stacked bar chart showing request counts by status, and department breakdown chart showing request distribution by department. Integrate both charts into the analytics page layout.

Purpose: Enable users to visualize workflow progress (pending/approved/rejected at each status) and understand which departments generate the most requests.
Output: Chart utilities, workflow pipeline chart component, department breakdown chart component, both integrated into analytics page.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-analytics-dashboard/12-analytics-CONTEXT.md
@.planning/phases/12-analytics-dashboard/12-RESEARCH.md
@.planning/phases/12-analytics-dashboard/12-01-SUMMARY.md
@.planning/phases/12-analytics-dashboard/12-02-SUMMARY.md
@.planning/phases/11-mobile-responsive-design/11-05-SUMMARY.md
@src/types/analytics.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared chart utilities</name>
  <files>src/components/analytics/chart-utils.ts</files>
  <action>Create src/components/analytics/chart-utils.ts with:

1. STATUS_COLORS constant object:
   - pending: '#eab308' (yellow, matches Tailwind yellow-500)
   - approved: '#22c55e' (green, matches Tailwind green-500)
   - rejected: '#ef4444' (red, matches Tailwind red-500)
   These colors must match status colors used throughout the app.

2. DEPT_COLORS array of distinct colors for department visualization:
   - ['#3b82f6', '#8b5cf6', '#ec4899', '#f97316', '#14b8a6', '#6366f1']
   Use consistent colors that work well with white/dark backgrounds.

3. Export ChartTooltip component (optional):
   - Custom tooltip wrapper with Tailwind styling
   - Background color, border, rounded corners for better visibility

All constants must be exported for use in chart components.
</action>
  <verify>TypeScript compilation succeeds. STATUS_COLORS and DEPT_COLORS exported correctly.</verify>
  <done>Shared chart utilities created with consistent color constants</done>
</task>

<task type="auto">
  <name>Task 2: Create Workflow Pipeline Chart component</name>
  <files>src/components/analytics/workflow-pipeline-chart.tsx</files>
  <action>Create src/components/analytics/workflow-pipeline-chart.tsx as stacked bar chart:

1. Import from recharts: BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer, Cell
2. Accept prop: data (WorkflowPipelineSegment[])
3. Transform data for Recharts:
   - Each status (ImprovementRequest, SentToEngineer, etc.) becomes a bar
   - Stacked with pending, approved, rejected segments
4. Render ResponsiveContainer with width="100%" and height={300} or aspect={aspect ratio}
5. Configure BarChart:
   - data prop: transformed data
   - margin: top: 20, right: 30, left: 20, bottom: 5
   - CartesianGrid with strokeDasharray="3 3"
   - XAxis with dataKey="step" (status name)
   - YAxis
   - Tooltip (custom or default)
   - Legend
   - Three Bar elements:
     - Bar dataKey="approved" stackId="status" fill={STATUS_COLORS.approved} name="Approved"
     - Bar dataKey="pending" stackId="status" fill={STATUS_COLORS.pending} name="Pending"
     - Bar dataKey="rejected" stackId="status" fill={STATUS_COLORS.rejected} name="Rejected"
6. Handle empty data: If data array is empty, show "No data available" message

Chart must use ResponsiveContainer for mobile responsiveness (per RESEARCH.md Pattern 2).
</action>
  <verify>Chart renders with stacked bars. Colors match STATUS_COLORS. Empty data shows message. Responsive behavior on resize.</verify>
  <done>Workflow pipeline chart created showing status breakdown with stacked bars</done>
</task>

<task type="auto">
  <name>Task 3: Create Department Breakdown Chart component</name>
  <files>src/components/analytics/department-breakdown-chart.tsx</files>
  <action>Create src/components/analytics/department-breakdown-chart.tsx as pie/horizontal bar chart:

**Use Pie Chart (per CONTEXT.md decision):**

1. Import from recharts: PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer
2. Accept prop: data (Array<{name: string, value: number}>)
3. Render ResponsiveContainer with width="100%" and height={300}
4. Configure PieChart with Pie element:
   - data prop: department data
   - cx="50%", cy="50%"
   - labelLine={false}
   - label={({ name, percent }) => `${name} ${(percent * 100).toFixed(0)}%`}
   - outerRadius={80}
   - dataKey="value"
   - Map each department to Cell with unique color from DEPT_COLORS array
   - Tooltip and Legend
5. Handle empty data: Show "No departments with requests" message

**Alternative approach:** Use horizontal BarChart if Pie chart is too cluttered for many departments.

Chart must be responsive with ResponsiveContainer.
</action>
  <verify>Chart renders with department data. Colors are distinct per department. Percentage labels visible on chart. Responsive behavior.</verify>
  <done>Department breakdown chart created showing request distribution by department</done>
</task>

<task type="auto">
  <name>Task 4: Integrate charts into AnalyticsPage</name>
  <files>src/components/analytics/analytics-page.tsx</files>
  <action>Update src/components/analytics/analytics-page.tsx to display the charts:

1. Import WorkflowPipelineChart and DepartmentBreakdownChart components
2. After SummaryCards, add two Card components in a vertical stack:
   - Card 1: Workflow Pipeline Chart
     - CardHeader: title "Workflow Pipeline"
     - CardContent: <WorkflowPipelineChart data={data.pipeline} />
   - Card 2: Department Breakdown Chart
     - CardHeader: title "Department Breakdown"
     - CardContent: <DepartmentBreakdownChart data={data.departments} />
3. Use Grid or vertical stack layout with gap-4 between cards
4. Ensure mobile responsiveness: Single column on mobile, maintain chart proportions

Layout must be: SummaryCards → Pipeline Chart → Department Breakdown Chart → (placeholder for time metrics)
</action>
  <verify>Analytics page renders both charts below summary cards. Charts display correct data from data.state. Mobile layout shows single column.</verify>
  <done>Charts integrated into analytics page layout</done>
</task>

</tasks>

<verification>
- [ ] Workflow pipeline chart shows stacked bars with correct status breakdown
- [ ] Status colors match app-wide colors (green=approved, yellow=pending, red=rejected)
- [ ] Department breakdown chart shows distribution by department with percentage labels
- [ ] Charts use ResponsiveContainer and resize correctly on window resize
- [ ] Charts work on mobile screens (test with browser devtools mobile view)
- [ ] Empty data handled gracefully with informative messages
- [ ] Chart tooltips show detailed values on hover
</verification>

<success_criteria>
1. Workflow pipeline chart displays request counts by status with stacked bars
2. Department breakdown chart shows request distribution by department
3. Both charts are responsive and work on mobile screens
4. Chart colors are consistent with app-wide status colors
5. Empty data shows informative messages without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-dashboard/12-03-SUMMARY.md`
</output>
