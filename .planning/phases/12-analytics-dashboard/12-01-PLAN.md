---
phase: 12-analytics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types/analytics.ts
  - src/server-actions/analytics.ts
autonomous: true

must_haves:
  truths:
    - Recharts library is installed for data visualization
    - Analytics type definitions are available throughout the app
    - Server Actions return valid analytics data structure
    - getAnalyticsData handles default 30-day filter correctly
  artifacts:
    - path: src/types/analytics.ts
      provides: Type definitions for analytics data structures
      contains: AnalyticsData, AnalyticsFilters, WorkflowPipelineSegment, TimeMetrics, SummaryMetrics
    - path: src/server-actions/analytics.ts
      provides: Server Actions for analytics data fetching
      exports: getAnalyticsData, getAnalyticsFilters
  key_links:
    - from: src/server-actions/analytics.ts
      to: prisma.request
      via: database query with filters
      pattern: prisma\\.request\\.(findMany|groupBy)
    - from: src/server-actions/analytics.ts
      to: src/types/analytics.ts
      via: AnalyticsFilters type import
      pattern: import.*AnalyticsFilters
---

<objective>
Install Recharts library, create analytics type definitions, and implement Server Actions for data fetching. Establish the data layer foundation for the analytics dashboard.

Purpose: Provide the core infrastructure (types and data fetching) that all analytics components will depend on.
Output: Recharts library installed, type definitions created, Server Actions for fetching analytics data with filtering support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-analytics-dashboard/12-analytics-CONTEXT.md
@.planning/phases/12-analytics-dashboard/12-RESEARCH.md
@src/server-actions/dashboard.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts library</name>
  <files>package.json</files>
  <action>Run npm install recharts to add the data visualization library. This is the only external dependency needed for the analytics dashboard.
  Recharts version: 2.x (latest stable).
  Do NOT install any other libraries - date-fns and Radix UI components are already installed.
  </action>
  <verify>Check package.json includes recharts in dependencies with version ^2.x</verify>
  <done>Recharts library installed and available for use in analytics components</done>
</task>

<task type="auto">
  <name>Task 2: Create analytics type definitions</name>
  <files>src/types/analytics.ts</files>
  <action>Create src/types/analytics.ts with the following TypeScript interfaces:

1. AnalyticsFilters: DateRangePreset ('7days' | '30days' | '90days' | 'all'), departmentId (string | undefined), status (string | undefined), requesterId (string | undefined)
2. WorkflowPipelineSegment: step (string), pending (number), approved (number), rejected (number)
3. TimeMetrics: avgPerRequest (number), avgPerApprovalLevel (number), medianPerRequest (number), minPerRequest (number), maxPerRequest (number)
4. SummaryMetrics: totalRequests (number), pendingRequests (number), avgApprovalTime (number in days), approvalRate (percentage 0-100)
5. AnalyticsData: pipeline (WorkflowPipelineSegment[]), departments (Array<{name: string, value: number}>), timeMetrics (TimeMetrics), summary (SummaryMetrics)

All interfaces must be exported.
</action>
  <verify>TypeScript compilation succeeds with tsc --noEmit, all interfaces exported</verify>
  <done>Type definitions created with all required interfaces for analytics data structures</done>
</task>

<task type="auto">
  <name>Task 3: Create Server Actions for analytics data</name>
  <files>src/server-actions/analytics.ts</files>
  <action>Create src/server-actions/analytics.ts with two Server Actions:

1. getAnalyticsData(filters: AnalyticsFilters): Returns AnalyticsData
   - Check authentication using @clerk/nextjs/server auth()
   - Build Prisma where clause from filters (isDeleted: false always)
   - Use date-fns subDays, startOfDay, endOfDay for date range calculation
   - Pipeline: Group requests by status, count each status
   - Departments: Group requests by department name, count each
   - Time metrics: Fetch requests with approvedAt, calculate avg/median/min/max in days using Date difference
   - Summary: Calculate total requests, pending count, avg approval time, approval rate (approved/total * 100)
   - Handle empty data case: Return zero values for all metrics

2. getAnalyticsFilters(): Returns array of departments {id: string, name: string} and users {id: string, name: string}
   - Check authentication
   - Fetch all active departments and users for filter dropdowns

Both functions must be marked with 'use server' directive.
</action>
  <verify>Test getAnalyticsData with default filters returns valid AnalyticsData structure with pipeline, departments, timeMetrics, summary. TypeScript compilation succeeds.</verify>
  <done>Server Actions created that fetch analytics data with filtering support</done>
</task>

</tasks>

<verification>
- [ ] Recharts library installed successfully
- [ ] Type definitions compile without errors
- [ ] Server Actions return valid data structure for default 30-day filter
- [ ] getAnalyticsData handles empty data gracefully (returns zeros)
- [ ] getAnalyticsFilters returns departments and users for dropdowns
</verification>

<success_criteria>
1. Recharts library installed and available in package.json
2. Analytics type definitions exported and TypeScript-valid
3. getAnalyticsData Server Action returns complete AnalyticsData structure
4. getAnalyticsFilters Server Action returns departments and users
5. All type definitions match actual data structures returned by Server Actions
</success_criteria>

<output>
After completion, create `.planning/phases/12-analytics-dashboard/12-01-SUMMARY.md`
</output>
