---
phase: 04-engineering-solutions
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server-actions/solutions.ts
  - src/components/requests/request-detail-modal.tsx
autonomous: false
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Engineering approvers see Approve and Reject buttons when they are in custom approval chain (regardless of role)"
    - "Non-engineering users selected as custom approvers can see and use approval buttons"
    - "Hierarchy-based approval buttons still only work for engineering users with correct level"
  artifacts:
    - path: "src/server-actions/solutions.ts"
      provides: "Solution approval authorization logic that allows custom chain approvers of any role"
      lines: [503-527]
    - path: "src/components/requests/request-detail-modal.tsx"
      provides: "Button rendering conditions without role restrictions for custom approvers"
      lines: [418, 477-496]
  key_links:
    - from: "src/server-actions/solutions.ts"
      to: "prisma.solutionApproval"
      via: "requiredApproverId match without role filter for custom chains"
      pattern: "requiredApproverId.*userId"
    - from: "src/components/requests/request-detail-modal.tsx"
      to: "solutions.ts canUserApproveSolution"
      via: "Button visibility based on approval eligibility not role"
      pattern: "canApproveSolution.*userRole"
---

<objective>
Fix approval button visibility for custom approvers by removing role restrictions for custom approval chains.

Purpose: Custom approval chains should allow selecting approvers from any department, but current code restricts button visibility and approval checks to engineering role only. This plan removes role restrictions for custom chains while maintaining them for hierarchy-based approvals.

Output: Working approval buttons for all custom chain approvers (engineering and non-engineering).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/engineering-approver-buttons.md
@src/server-actions/solutions.ts
@src/components/requests/request-detail-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove role restriction for custom chain approval checks</name>
  <files>src/server-actions/solutions.ts</files>
  <action>
    In the canUserApproveSolution function (lines 503-513), remove the engineering role check for custom approval chains to allow non-engineering users in custom chains:

    Current code at line 503:
    ```typescript
    if (user.role === UserRole.engineering) {
      // Check for custom chain approval
      approval = await prisma.solutionApproval.findFirst({...})
    }
    ```

    Change to:
    ```typescript
    // Check for custom chain approval (any role can be in custom chain)
    approval = await prisma.solutionApproval.findFirst({
      where: {
        solutionId,
        status: 'pending',
        isCustomChain: true,
        requiredApproverId: userId,
      },
      orderBy: { order: 'asc' },
    })
    ```

    Keep the hierarchy-based approval role check (lines 517-527) unchanged - hierarchy approvals should still only work for engineering users.

    Why: Custom approval chains allow selecting any active user (including non-engineering users like finance managers) as approvers, but the role check prevents non-engineering users from being validated. Security comes from requiredApproverId match, not role. This enables non-engineering custom approvers to pass authorization checks.
  </action>
  <verify>grep -A 10 "Check for custom chain approval" src/server-actions/solutions.ts | grep -v "if (user.role === UserRole.engineering)"</verify>
  <done>Custom chain approval lookup executes for all users (including non-engineering), hierarchy approval lookup still requires engineering role</done>
</task>

<task type="auto">
  <name>Remove engineering role requirement from approval button rendering</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    At line 418, change the button rendering condition from:
    ```typescript
    {canApproveSolution && userRole === 'engineering' && (
    ```

    To:
    ```typescript
    {canApproveSolution && (
    ```

    Why: The canApproveSolution check already validates approval eligibility (either in custom chain OR engineering hierarchy). Adding the role check prevents non-engineering custom approvers from seeing approval buttons. The security validation happens in canUserApproveSolution, not in UI rendering. This allows non-engineering users in custom chains to see and use approval buttons.
  </action>
  <verify>grep -n "canApproveSolution &&" src/components/requests/request-detail-modal.tsx | grep -v "userRole === 'engineering'"</verify>
  <done>Approval buttons display based on canApproveSolution result without additional role filtering, enabling non-engineering custom approvers to see buttons</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fixed approval button visibility for custom approvers:
    1. Removed engineering role restriction from custom chain approval checks (solutions.ts)
    2. Removed engineering role requirement from approval button rendering (request-detail-modal.tsx)
  </what-built>
  <how-to-verify>
    Test 1: Engineering user in custom approval chain
    - Submit solution with custom chain including an engineering user
    - Login as that engineering user
    - Open request detail modal
    - Expected: See "Approve" and "Reject" buttons

    Test 2: Non-engineering user in custom approval chain
    - Submit solution with custom chain including a non-engineering user (e.g., manager from finance dept)
    - Login as that non-engineering user
    - Open request detail modal
    - Expected: See "Approve" and "Reject" buttons

    Test 3: Engineering user in hierarchy-based approval
    - Submit solution without custom chain (uses hierarchy)
    - Login as engineering user at correct level
    - Open request detail modal
    - Expected: See "Approve" and "Reject" buttons (only for engineering users)

    Test 4: User NOT in approval chain
    - Login as user who is not in approval chain
    - Open request detail modal
    - Expected: NO approval buttons shown
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Non-engineering custom chain approvers can see and use approval buttons
2. Engineering custom chain approvers can see and use approval buttons
3. Hierarchy-based approvals still restricted to engineering users at correct levels
4. Users not in approval chains cannot see approval buttons (security validation)
</verification>

<success_criteria>
- Custom approval chains work cross-department as designed
- Approval buttons display based on chain membership, not role
- Non-engineering approvers in custom chains can approve/reject
- Hierarchy approvals remain engineering-only as intended
- No security bypasses - approval eligibility still validated server-side
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-15-SUMMARY.md`
</output>
