---
phase: 04-engineering-solutions
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx
  - src/components/solutions/solution-form.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Custom approval chain picker shows ALL active users in the system"
    - "Picker is not filtered to engineering department only"
    - "Non-engineering users can be selected as approvers"
  artifacts:
    - path: "src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx"
      provides: "Page component that queries all active users instead of just engineering users"
    - path: "src/components/solutions/solution-form.tsx"
      provides: "Form component accepting allUsers prop (renamed from engineeringUsers)"
  key_links:
    - from: "src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx"
      to: "src/components/solutions/solution-form.tsx"
      via: "allUsers prop passed to SolutionForm component"
      pattern: "allUsers=\\{allUsers\\}"
---

<objective>
Fix custom approval chain to show all users, not just engineering users

Purpose: The custom approval chain picker currently filters to engineering department users only. Business requirement is to show ALL active users in the system, allowing engineering users to select approvers from any department (e.g., finance, management) for their custom approval chain.

Output: Custom approval picker displays all active users regardless of department
</objective>

<execution_context>
@./.claud/get-shit-done/workflows/execute-plan.md
@./.claud/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/approval-chain-filter.md

# Existing implementation
@src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx
@src/components/solutions/solution-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Update page.tsx to query all active users instead of engineering users only</name>
  <files>src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx</files>
  <action>
    Modify src/app/(dashboard)/engineering/solutions/[requestId]/page.tsx:

    1. Remove lines 63-71 (engineering department lookup):
       Delete the entire block that queries for engineeringDept:
       ```typescript
       const engineeringDept = await prisma.department.findFirst({
         where: { type: 'ENGINEERING' },
         select: { id: true },
       })

       if (!engineeringDept) {
         throw new Error('Engineering department not found')
       }
       ```

    2. Replace engineeringUsers query (lines 73-85) with allUsers query:
       ```typescript
       const allUsers = await prisma.user.findMany({
         where: {
           isActive: true,  // Only filter by active status, no department filter
         },
         select: {
           id: true,
           name: true,
           email: true,
           level: true,
         },
         orderBy: { name: 'asc' },  // Sort by name for all users
       })
       ```

    3. Update prop passed to SolutionForm (line 117):
       - Current: `engineeringUsers={engineeringUsers}`
       - New: `allUsers={allUsers}`

       This removes the department filter, allowing all active users to be available for custom approval selection.
  </action>
  <verify>Query selects all active users regardless of department</verify>
  <done>Page queries all active users and passes to SolutionForm as allUsers prop</done>
</task>

<task type="auto">
  <name>Update SolutionForm to accept allUsers prop instead of engineeringUsers</name>
  <files>src/components/solutions/solution-form.tsx</files>
  <action>
    Modify src/components/solutions/solution-form.tsx for consistent naming:

    1. Update SolutionFormProps interface (lines 52-57):
       - Current: `engineeringUsers: Array<{ id: string; name: string; email: string; level: number | null }>`
       - New: `allUsers: Array<{ id: string; name: string; email: string; level: number | null }>`

    2. Update function parameter (line 71):
       - Current: `engineeringUsers,`
       - New: `allUsers,`

    3. Update customApprovers mapping (line 250):
       - Current: `.map((id: string) => engineeringUsers.find((u: ...) => u.id === id))`
       - New: `.map((id: string) => allUsers.find((u: ...) => u.id === id))`

    4. Update CustomApprovalPicker users prop (line 473):
       - Current: `users={engineeringUsers}`
       - New: `users={allUsers}`

    The renaming makes the code more semantically accurate - the prop contains all users, not just engineering users.
  </action>
  <verify>Component accepts allUsers prop and passes to CustomApprovalPicker</verify>
  <done>SolutionForm interface and implementation updated to use allUsers naming</done>
</task>

</tasks>

<verification>
1. Open solution submission form as engineering user
2. Toggle "Custom Approval Chain" switch to ON
3. Click "Add Approver" dropdown
4. Verify dropdown shows users from ALL departments (not just engineering)
5. Select a non-engineering user as approver
6. Verify user appears in approval chain list
</verification>

<success_criteria>
- Custom approval picker shows users from all departments
- Non-engineering users can be selected as approvers
- Dropdown is sorted alphabetically by name
- No department filtering in user query
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-10-SUMMARY.md`
</output>
