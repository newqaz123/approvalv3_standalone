---
phase: 04-engineering-solutions
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/requests/request-detail-modal.tsx
autonomous: false
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Engineering users at top level of hierarchy can see Solution Submit button in request detail modal"
    - "Submit button visibility works correctly even when modal opens before authentication completes"
    - "userRole state updates dynamically when user authentication data changes"
  artifacts:
    - path: "src/components/requests/request-detail-modal.tsx"
      provides: "userRole state management with proper dependency tracking"
      lines: [66-72]
  key_links:
    - from: "src/components/requests/request-detail-modal.tsx"
      to: "userRole state"
      via: "useState and useEffect for reactive updates when user authentication completes"
      pattern: "useState.*userRole|useEffect.*user"
---

<objective>
Fix Solution Submit button visibility for engineering users by converting userRole to proper state management.

Purpose: userRole was extracted directly from Clerk user object without state management. When modal opens before authentication completes, userRole remains stale/undefined, preventing the submit button from appearing. This plan implements proper state management with useEffect dependency tracking to ensure userRole updates when user data loads.

Output: Engineering users at top level can reliably see Solution Submit button in request detail modal.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/resolved/submit-button-visibility.md
@src/components/requests/request-detail-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Convert userRole to state with proper dependency tracking</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Fix the submit button visibility issue by converting userRole from direct extraction to state:

    1. At line 66, change:
    ```typescript
    const userRole = user?.publicMetadata?.role as string | undefined;
    ```

    To:
    ```typescript
    const [userRole, setUserRole] = useState<string | undefined>(
      user?.publicMetadata?.role as string | undefined
    );
    ```

    2. Add useEffect after line 72 to update userRole when user data changes:
    ```typescript
    useEffect(() => {
      setUserRole(user?.publicMetadata?.role as string | undefined);
    }, [user?.id, user?.publicMetadata?.role]);
    ```

    Why: userRole was extracted directly from Clerk user object without state management. When modal opens before authentication completes, userRole remains stale/undefined. State with useEffect ensures userRole updates when user data loads.

    Reference: .planning/debug/resolved/submit-button-visibility.md
  </action>
  <verify>grep -A 5 "const \[userRole, setUserRole\]" src/components/requests/request-detail-modal.tsx | grep "useState"</verify>
  <done>userRole is managed as state and updates when user authentication data changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fixed submit button visibility for engineering users:
    1. Converted userRole from direct extraction to useState
    2. Added useEffect to update userRole when user authentication data changes
  </what-built>
  <how-to-verify>
    Test 1: Top-level engineering user submit button
    - Login as engineering user at top level of hierarchy
    - Open request with status "SentToEngineer"
    - Expected: See green "Submit Solution" button

    Test 2: Modal opens before authentication completes
    - Navigate to request detail quickly after login
    - Wait for authentication to complete
    - Expected: Submit button appears after user data loads (may require modal close/reopen)

    Test 3: Non-engineering user
    - Login as non-engineering user
    - Open request with status "SentToEngineer"
    - Expected: NO submit button (only engineering users at top level see it)

    Test 4: Engineering user not at top level
    - Login as engineering user at middle level
    - Open request with status "SentToEngineer"
    - Expected: NO submit button (only top level sees it)
  </how-to-verify>
  <resume-signal>Type "approved" if submit button appears correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Engineering users at top level of hierarchy can see Solution Submit button in modal
2. Submit button visibility works when modal opens before authentication completes
3. Non-engineering users do not see submit button
4. Engineering users not at top level do not see submit button
5. userRole updates reactively when user authentication state changes
</verification>

<success_criteria>
- Submit button reliably visible for eligible engineering users
- userRole state properly managed with useState and useEffect
- Dependency tracking ensures updates when user data loads
- No race conditions between modal render and authentication completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-19-SUMMARY.md`
</output>
