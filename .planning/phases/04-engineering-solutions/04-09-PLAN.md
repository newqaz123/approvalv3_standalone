---
phase: 04-engineering-solutions
plan: 09
type: execute
wave: 4
depends_on: ["04-08"]
files_modified:
  - src/lib/schemas/solution-schemas.ts
  - src/server-actions/solutions.ts
  - src/components/solutions/solution-form.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Cost Estimate field is optional (can be left empty)"
    - "Cost Estimate doesn't auto-fill with 0"
    - "No 'Cost must be greater than 0' error when field is empty"
    - "Currency dropdown has THB, USD, EUR options"
  artifacts:
    - path: "src/lib/schemas/solution-schemas.ts"
      provides: "Updated schema with optional costEstimate and expanded currency enum"
    - path: "src/components/solutions/solution-form.tsx"
      provides: "Form with corrected default values and onChange handler"
  key_links:
    - from: "src/components/solutions/solution-form.tsx"
      to: "src/lib/schemas/solution-schemas.ts"
      via: "solutionFormSchema import and type inference"
      pattern: "solutionFormSchema.*z\\.object"
---

<objective>
Fix cost estimate field validation and add missing currency options

Purpose: Cost Estimate field has three issues: (1) auto-fills with 0, (2) validation error shows immediately, (3) onChange converts empty input to 0. Currency dropdown only shows THB, missing USD and EUR options from business requirements.

Output: Optional cost estimate field with THB/USD/EUR currency selection
</objective>

<execution_context>
@./.claud/get-shit-done/workflows/execute-plan.md
@./.claud/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/cost-estimate-issues.md

# Existing implementation
@src/lib/schemas/solution-schemas.ts
@src/components/solutions/solution-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Update schema to make costEstimate optional and add currency options</name>
  <files>src/lib/schemas/solution-schemas.ts</files>
  <action>
    Modify submitSolutionSchema in src/lib/schemas/solution-schemas.ts:

    1. Change costEstimate from required to optional:
       - Current: `costEstimate: z.number().positive().min(0.01)`
       - New: `costEstimate: z.number().positive().min(0.01).optional()`

       The .optional() allows undefined, which means field can be left empty. When provided, validation still requires positive value > 0.01.

    2. Expand currency enum to include USD and EUR:
       - Current: `currency: z.enum(['THB']).default('THB')`
       - New: `currency: z.enum(['THB', 'USD', 'EUR']).default('THB')`

    Note: This schema file is used by both the server action and client form, so both will be updated by this change.
  </action>
  <verify>Schema allows undefined costEstimate and accepts THB/USD/EUR currencies</verify>
  <done>Schema updated with optional costEstimate and expanded currency enum</done>
</task>

<task type="auto">
  <name>Fix form default values and onChange handler</name>
  <files>src/components/solutions/solution-form.tsx</files>
  <action>
    Modify src/components/solutions/solution-form.tsx to fix three issues:

    1. Update solutionFormSchema to match lib/schemas (lines 36-48):
       - Change costEstimate to: `z.number().positive().min(0.01).optional()`
       - Change currency to: `z.enum(['THB', 'USD', 'EUR'])`

    2. Change defaultValues for costEstimate (line 83):
       - Current: `costEstimate: 0`
       - New: `costEstimate: undefined`

       Using undefined instead of 0 prevents the initial validation error.

    3. Fix onChange handler to preserve undefined (lines 351-354):
       - Current: `field.onChange(isNaN(value) ? 0 : value)`
       - New: `field.onChange(isNaN(value) ? undefined : value)`

       Converting to undefined instead of 0 allows field to be cleared without triggering validation error.

    4. Add USD and EUR SelectItem options (after line 378):
       ```tsx
       <SelectItem value="USD">USD ($)</SelectItem>
       <SelectItem value="EUR">EUR (€)</SelectItem>
       ```

    5. Update FormLabel to remove asterisk (line 344):
       - Current: `<FormLabel>Cost Estimate *</FormLabel>`
       - New: `<FormLabel>Cost Estimate</FormLabel>`

       Field is optional, so remove visual indicator of required field.
  </action>
  <verify>Form loads with empty cost estimate, currency dropdown shows 3 options</verify>
  <done>Form initialized correctly with optional costEstimate and expanded currency options</done>
</task>

</tasks>

<verification>
1. Load solution submission form - Cost Estimate field should be empty (not "0")
2. Leave Cost Estimate empty - no validation error should appear
3. Enter value in Cost Estimate then clear it - field should accept empty state
4. Click Currency dropdown - should show THB, USD, EUR options
5. Select USD or EUR - option should be selected
6. Submit form with empty Cost Estimate - should succeed
</verification>

<success_criteria>
- Cost Estimate field initializes as empty (no "0" pre-filled)
- No validation error when field is empty
- Currency dropdown has THB, USD, EUR options
- Form submits successfully with empty Cost Estimate
- Entering and clearing cost value works without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-09-SUMMARY.md`
</output>
