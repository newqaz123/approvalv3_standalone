---
phase: 04-engineering-solutions
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/solutions/custom-approval-picker.tsx
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Custom approval chain up/down buttons are clickable and responsive"
    - "Buttons have clear visual affordance with larger click targets"
    - "Disabled buttons are hidden instead of shown in disabled state"
  artifacts:
    - path: "src/components/solutions/custom-approval-picker.tsx"
      provides: "Improved up/down button usability"
      contains: "cursor-pointer class, larger click targets, conditional visibility"
  key_links:
    - from: "custom-approval-picker.tsx"
      to: "button onClick handlers"
      via: "handleMoveUp/handleMoveDown properly wired"
      pattern: "onClick.*handleMove"
---

<objective>
Fix custom approval chain up/down button usability issues to make reordering approvers intuitive and responsive.

Purpose: User reported "can select any user but can't press up/down button for arranging." Root cause analysis reveals multiple UX issues: (1) small 32x32px click targets with ghost variant styling, (2) missing explicit cursor-pointer class for visual affordance, (3) edge cases where buttons appear disabled for valid reasons (1 approver = both disabled), (4) ghost variant has low visual contrast.

Output: Up/down buttons are clearly clickable, have adequate click targets (36x36px minimum), show clear cursor affordance, and disabled buttons are hidden to reduce visual clutter.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap Context (from UAT retest)
- User reported: "can seelct any user but can't press up/down botton for arranging."
- Root cause: Small 32x32px click targets, ghost variant with low contrast, no explicit cursor-pointer, disabled buttons shown

# Existing Implementation
- custom-approval-picker.tsx:162-181 uses variant="ghost", size="sm", className="h-8 w-8 p-0"
- Ghost variant in button.tsx:20 has "hover:bg-accent hover:text-accent-foreground" but no explicit cursor-pointer
- Disabled state handled by disabled={index === 0 || disabled} for up button

# Button Component Reference
- button.tsx:8 has "disabled:pointer-events-none disabled:opacity-50" but no cursor-pointer on ghost variant
</context>

<tasks>

<task type="auto">
  <name>Improve up/down button visual affordance and click targets</name>
  <files>src/components/solutions/custom-approval-picker.tsx</files>
  <action>
    Update the up/down buttons (lines 162-181) with the following improvements:

    1. **Increase click target size from 32x32px to 36x36px:**
       Change `className="h-8 w-8 p-0"` to `className="h-9 w-9 p-0"` for both up and down buttons

    2. **Add explicit cursor-pointer class:**
       Append `cursor-pointer` to className - this ensures users see pointer cursor on hover

    3. **Change from ghost to outline variant for better visual affordance:**
       Change `variant="ghost"` to `variant="outline"` - outline has visible border making it more obvious as a clickable button

    4. **Hide disabled buttons instead of showing them disabled:**
       - For up button: Change condition from `disabled={index === 0 || disabled}` to `className={cn("...", (index === 0 || disabled) && "hidden")}`
       - For down button: Change condition from `disabled={index === selectedIds.length - 1 || disabled}` to `className={cn("...", (index === selectedIds.length - 1 || disabled) && "hidden")}`
       - Remove the disabled prop entirely since we're hiding the buttons

    5. **Add console.log debugging to handleMoveUp/handleMoveDown:**
       Add at the start of each function:
       ```typescript
       console.log('handleMoveUp called for index:', index) // or handleMoveDown
       ```

    Updated button code should look like:
    ```tsx
    <Button
      type="button"
      variant="outline"
      size="sm"
      className={cn(
        "h-9 w-9 p-0 cursor-pointer",
        (index === 0 || disabled) && "hidden"
      )}
      onClick={() => handleMoveUp(index)}
    >
      <ChevronUp className="h-4 w-4" />
    </Button>
    ```

    Note: Keep the remove (X) button as-is with variant="ghost" and text-destructive since removal functionality is different from reordering.
  </action>
  <verify>grep -A2 "ChevronUp\|ChevronDown" src/components/solutions/custom-approval-picker.tsx shows updated className with h-9 w-9 cursor-pointer and hidden logic</verify>
  <done>Up/down buttons have larger click targets, outline variant, cursor-pointer, and are hidden when disabled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Custom approval chain up/down buttons improved with better visual affordance and usability</what-built>
  <how-to-verify>
    1. Login as an engineering user
    2. Navigate to /engineering and find a request
    3. Click "Submit Solution"
    4. Enable "Use Custom Approval Chain" toggle
    5. Add at least 3 approvers
    6. Verify: First approver has only down arrow (up arrow hidden)
    7. Verify: Last approver has only up arrow (down arrow hidden)
    8. Verify: Middle approvers have both up and down arrows
    9. Click up/down arrows to reorder approvers
    10. Verify: Buttons are clearly visible with outline variant
    11. Verify: Cursor changes to pointer when hovering over buttons
    12. Verify: Approver order changes when buttons are clicked
    13. Check browser console for handleMoveUp/handleMoveDown log messages confirming clicks register
  </how-to-verify>
  <resume-signal>Type "approved" if up/down buttons work for reordering approvers, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Buttons use h-9 w-9 (36x36px) instead of h-8 w-8 (32x32px)
2. Buttons use variant="outline" instead of variant="ghost"
3. cursor-pointer class added to button className
4. Disabled buttons use hidden class instead of disabled prop
5. console.log statements added to handleMoveUp/handleMoveDown
6. Button click handlers fire correctly (verified via console logs)
</verification>

<success_criteria>
Custom approval chain approvers can be reordered with responsive up/down buttons that have clear visual affordance and adequate click targets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-14-SUMMARY.md`
</output>
