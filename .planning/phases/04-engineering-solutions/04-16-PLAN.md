---
phase: 04-engineering-solutions
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/requests/request-detail-modal.tsx
  - src/components/solutions/solution-detail.tsx
autonomous: false
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Solution Approval Progress shows numbered badges for each approval step (1, 2, 3...)"
    - "Current pending approval has blue background highlighting (border-blue-300 bg-blue-50)"
    - "Completed approvals show different visual style (green/gray)"
    - "Approval chain has clear visual hierarchy showing progression"
  artifacts:
    - path: "src/components/requests/request-detail-modal.tsx"
      provides: "Solution Approval Progress section with proper visual hierarchy"
      lines: [438-469]
    - path: "src/components/solutions/solution-detail.tsx"
      provides: "Reference implementation with numbered badges and blue highlighting"
      lines: [158-257]
  key_links:
    - from: "src/components/requests/request-detail-modal.tsx"
      to: "src/components/solutions/solution-detail.tsx"
      via: "Copy proper implementation pattern for approval progress display"
      pattern: "Solution Approval Progress section"
---

<objective>
Update Solution Approval Progress section in request detail modal to display approval steps with numbered badges and blue background highlighting for current pending approval.

Purpose: Current implementation uses basic card styling without visual hierarchy. Users cannot see numbered steps or identify which approval is currently pending. The solution-detail.tsx component has proper implementation that should be replicated.

Output: Solution Approval Progress with clear numbered steps, visual hierarchy, and highlighted current approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/solution-approval-progress.md
@src/components/requests/request-detail-modal.tsx
@src/components/solutions/solution-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Replace Solution Approval Progress implementation with proper version</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Replace the Solution Approval Progress section (lines 438-469) with the proper implementation from solution-detail.tsx (lines 158-257):

    1. Copy the approval progress JSX structure from solution-detail.tsx that includes:
       - Numbered badges (1, 2, 3...) for each approval step
       - Conditional styling for approval status (pending/approved/rejected)
       - Blue background (border-blue-300 bg-blue-50) for current pending approval
       - Visual hierarchy with proper spacing and borders

    2. Ensure the copied code works in modal context:
       - Adjust variable names if needed (solution vs solutionApprovals)
       - Keep approval logic intact (sequential approvals, current pending highlight)
       - Maintain responsive design for modal width

    Key visual elements to include:
    ```typescript
    // Numbered badge
    <div className="flex h-6 w-6 items-center justify-center rounded-full bg-blue-600 text-xs font-semibold text-white">
      {index + 1}
    </div>

    // Current pending highlighting
    className="border-blue-300 bg-blue-50"

    // Completed approval styling
    className="border-green-300 bg-green-50"
    ```

    Why: User reported "Not show step of Solution Approval Progress. only show current 1 pending and no blue background." The solution-detail.tsx has working implementation with proper visual hierarchy.

    Reference: .planning/debug/solution-approval-progress.md
  </action>
  <verify>grep -A 5 "Solution Approval Progress" src/components/requests/request-detail-modal.tsx | grep -E "rounded-full|border-blue-300"</verify>
  <done>Solution Approval Progress shows numbered badges (1, 2, 3...) and blue background (border-blue-300 bg-blue-50) for current approval</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Replaced Solution Approval Progress section with proper implementation featuring:
    1. Numbered badges for each approval step (1, 2, 3...)
    2. Blue background highlighting for current pending approval
    3. Different visual styles for completed/pending/rejected approvals
    4. Clear visual hierarchy showing approval chain progression
  </what-built>
  <how-to-verify>
    Test 1: Single pending approval
    - Submit solution that enters approval chain
    - Open request detail modal
    - Expected: See "Solution Approval Progress" section with numbered badge "1" highlighted with blue background

    Test 2: Multiple approvals with one completed
    - Approve first step in chain
    - Refresh and open request detail modal
    - Expected:
      - Step 1: Shows "Approved" with green/gray styling
      - Step 2: Shows pending with blue background highlight

    Test 3: Multiple pending approvals
    - Submit solution with 3-approval custom chain
    - Open request detail modal
    - Expected: All 3 steps shown with numbers, only first step has blue highlight

    Test 4: All approvals completed
    - Complete entire approval chain
    - Open request detail modal
    - Expected: All steps show approved/rejected styling, no blue highlighting

    Visual checks:
    - [ ] Numbered badges visible (1, 2, 3...)
    - [ ] Current pending has blue background
    - [ ] Completed approvals have different styling
    - [ ] Approval order is clear (top to bottom)
  </how-to-verify>
  <resume-signal>Type "approved" if visual hierarchy is correct, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. All approval steps displayed with numbered badges
2. Current pending approval highlighted with blue background (border-blue-300 bg-blue-50)
3. Completed approvals have distinct visual styling (green/gray)
4. Approval chain order is clear (sequential top to bottom)
5. Section renders correctly in modal width constraints
</verification>

<success_criteria>
- Solution Approval Progress shows clear visual hierarchy
- Users can identify current pending approval at a glance
- Approval chain progression is visually obvious
- Implementation matches solution-detail.tsx pattern
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-16-SUMMARY.md`
</output>
