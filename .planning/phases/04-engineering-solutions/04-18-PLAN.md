---
phase: 04-engineering-solutions
plan: 18
type: execute
wave: 1
depends_on: [04-16]
files_modified:
  - src/components/requests/request-detail-modal.tsx
  - src/components/approvals/approval-progress.tsx
autonomous: false
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Single unified 'Approval Progress' section consolidates all approval types"
    - "Approval hierarchy steps displayed consistently using ApprovalProgress component"
    - "Activity History removed (redundant with approval progress timeline)"
    - "Modal layout organized by workflow phase: Engineering Solution → Final Approval → Completion"
    - "Visual clutter reduced with minimal, consistent design"
  artifacts:
    - path: "src/components/requests/request-detail-modal.tsx"
      provides: "Consolidated approval progress section with ApprovalProgress component"
      lines: [438-707]
    - path: "src/components/approvals/approval-progress.tsx"
      provides: "Proper hierarchy visualization component"
    - path: "src/components/solutions/solution-detail.tsx"
      provides: "Reference for minimal progress display"
  key_links:
    - from: "src/components/requests/request-detail-modal.tsx"
      to: "src/components/approvals/approval-progress.tsx"
      via: "Use ApprovalProgress component consistently for all approval types"
      pattern: "ApprovalProgress.*approvals"
---

<objective>
Redesign request detail modal to consolidate redundant approval progress sections into single unified display with minimal, consistent design using ApprovalProgress component.

Purpose: Modal contains three separate approval progress sections (Solution Approval Progress, Final Approval Progress, Approval Progress) with different styling creating visual clutter. Only regular approvals use ApprovalProgress component with proper hierarchy visualization. This plan consolidates into single section using consistent component, reduces redundancy, and improves visual hierarchy.

Output: Clean, minimal modal layout with unified approval progress display showing all approvals consistently.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/solution-modal-minimalism.md
@.planning/phases/04-engineering-solutions/04-16-PLAN.md
@src/components/requests/request-detail-modal.tsx
@src/components/approvals/approval-progress.tsx
@src/components/solutions/solution-detail.tsx
</context>

<tasks>

<task type="auto">
  <name>Consolidate approval progress sections into single unified display</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Redesign approval progress display in request detail modal (lines 438-707):

    1. Remove redundant sections:
       - Solution Approval Progress (lines 438-470) - will be integrated
       - Final Approval Progress (lines 619-650) - will be integrated
       - Activity History (lines 696-707) - remove as redundant (approval steps already shown)

    2. Create single unified "Approval Progress" section:
    ```typescript
    {solutionApprovals && solutionApprovals.length > 0 && (
      <div className="space-y-4">
        <h3 className="text-lg font-semibold">Engineering Solution Approval</h3>
        <ApprovalProgress approvals={solutionApprovals} />
      </div>
    )}

    {finalApprovals && finalApprovals.length > 0 && (
      <div className="space-y-4">
        <h3 className="text-lg font-semibold">Final Department Approval</h3>
        <ApprovalProgress approvals={finalApprovals} />
      </div>
    )}

    {requestApprovals && requestApprovals.length > 0 && !solutionApprovals && (
      <div className="space-y-4">
        <h3 className="text-lg font-semibold">Department Approval</h3>
        <ApprovalProgress approvals={requestApprovals} />
      </div>
    )}
    ```

    3. Import ApprovalProgress component:
    ```typescript
    import { ApprovalProgress } from '@/components/approvals/approval-progress'
    ```

    4. Ensure ApprovalProgress component can handle all approval types:
       - Check if ApprovalProgress needs updates for solution approvals
       - Ensure it displays custom chain approvals correctly
       - Verify it shows hierarchy visualization

    5. Organize by workflow phase (top to bottom):
       - Request details (always show)
       - Solution details (if exists)
       - Engineering Solution Approval (if in solution workflow)
       - Final Department Approval (if in final workflow)
       - Regular Department Approval (if in initial workflow)
       - Action buttons (approve/reject/submit)
       - Completion status (if completed)

    Why: User reported "Solution Approval Progress and Approval Progress and Activity History have to redesign for minimal." Consolidating reduces visual clutter and provides consistent hierarchy display. ApprovalProgress component already has proper visualization.

    Reference: .planning/debug/solution-modal-minimalism.md
    Reference: 04-16-PLAN.md (ensure dependency on Solution Approval Progress fix)
  </action>
  <verify>grep -c "Approval Progress" src/components/requests/request-detail-modal.tsx | grep -q "^1$" && grep -c "ApprovalProgress" src/components/requests/request-detail-modal.tsx | grep -q "[1-9]"</verify>
  <done>Single unified "Approval Progress" section (or clearly separated sections by type) using ApprovalProgress component consistently across all approval types</done>
</task>

<task type="auto">
  <name>Remove Activity History section (redundant with approval progress)</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Remove the Activity History section (lines 696-707) since approval progress already shows:
    - Who approved/rejected
    - When approval occurred
    - Approval comments
    - Approval status progression

    Activity History is redundant because:
    1. All approval actions are already visible in ApprovalProgress component
    2. Duplicate display creates visual clutter
    3. Non-approval activities (cancellation, completion) can be shown as simple status banners

    Replace Activity History with concise status indicators:
    - If cancelled: Show "Request Cancelled" banner with reason
    - If completed: Show "Request Completed" banner with completion note
    - If rejected: Show rejection reason inline in approval progress

    Keep activity log query for audit trail but don't display in modal (available via export in Phase 6).

    Verify Activity History section no longer exists in modal after consolidation.
  </action>
  <verify>grep -n "Activity History" src/components/requests/request-detail-modal.tsx | wc -l | grep -q "^0$"</verify>
  <done>Activity History section removed, approval progress provides complete timeline view, non-approval activities shown as status banners</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Redesigned modal with minimal, consolidated approval display:
    1. Single "Approval Progress" section using ApprovalProgress component consistently
    2. Removed redundant sections (Solution Approval Progress, Final Approval Progress, Activity History)
    3. Organized by workflow phase with clear headers
    4. Visual clutter reduced with consistent design
  </what-built>
  <how-to-verify>
    Test 1: Request in initial approval workflow
    - Open request with only department approvals
    - Expected: Single "Department Approval" section showing hierarchy steps
    - Expected: No duplicate approval sections

    Test 2: Request in solution approval workflow
    - Open request with submitted solution
    - Expected: "Engineering Solution Approval" section showing solution approval chain
    - Expected: Numbered steps, blue highlighting for current approval (from 04-16 fix)
    - Expected: NO separate "Solution Approval Progress" section

    Test 3: Request in final approval workflow
    - Open request with final approval initiated
    - Expected: "Final Department Approval" section showing final approval chain
    - Expected: Hierarchy visualization with proper connectors

    Test 4: Completed request
    - Open completed request
    - Expected: All approval sections show completed status
    - Expected: Green "Request Completed" banner
    - Expected: NO Activity History section

    Visual checks:
    - [ ] No redundant approval progress sections
    - [ ] Consistent styling across all approval types
    - [ ] Clear visual hierarchy (top to bottom by workflow phase)
    - [ ] Less visual clutter than before
    - [ ] Approval steps properly displayed with hierarchy
  </how-to-verify>
  <resume-signal>Type "approved" if modal is cleaner and consistent, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Single "Approval Progress" section (or clearly separated sections by type) using ApprovalProgress component
2. No redundant approval progress sections
3. Activity History removed (approval progress provides timeline)
4. Hierarchy visualization consistent across all approval types
5. Modal layout organized by workflow phase
6. Visual clutter reduced compared to previous implementation
</verification>

<success_criteria>
- Modal displays all approvals with consistent visual design
- Users can see approval progression clearly without clutter
- Hierarchy steps properly displayed (numbered, connectors, highlighting)
- Redundant sections eliminated
- Minimal, clean design that scales to multiple approval types
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-18-SUMMARY.md`
</output>
