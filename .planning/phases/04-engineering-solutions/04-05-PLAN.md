---
phase: 04-engineering-solutions
plan: 05
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - prisma/schema.prisma
  - src/server-actions/solutions.ts
  - src/server-actions/approvals.ts
  - src/components/solutions/final-approval-actions.tsx
  - src/components/requests/request-detail-modal.tsx
autonomous: true

must_haves:
  truths:
    - "Department users can initiate final approval from SendBackToRequester status"
    - "Final approval can use default hierarchy or custom approval chain"
    - "Department approvers can reject solution back to engineering"
    - "Final approval completion changes status to Completed"
  artifacts:
    - path: "src/server-actions/solutions.ts"
      provides: "Final approval initiation and completion"
      exports: ["initiateFinalApproval", "approveFinalApproval", "rejectFinalApproval"]
    - path: "src/components/solutions/final-approval-actions.tsx"
      provides: "UI for initiating and managing final approval"
      min_lines: 100
  key_links:
    - from: "src/components/solutions/final-approval-actions.tsx"
      to: "src/server-actions/solutions.ts"
      via: "initiateFinalApproval"
      pattern: "initiateFinalApproval"
---

<objective>
Implement final department approval workflow where requesters route solutions through their department hierarchy for final acceptance.

Purpose: Complete the three-stage approval loop with final department sign-off before marking as completed.
Output: Final approval UI and backend logic with custom chain support.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-CONTEXT.md
@.planning/phases/04-engineering-solutions/04-RESEARCH.md
@.planning/phases/04-engineering-solutions/04-01-SUMMARY.md
@.planning/phases/04-engineering-solutions/04-03-SUMMARY.md
@src/server-actions/solutions.ts
@src/server-actions/approvals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add final approval server actions</name>
  <files>src/server-actions/solutions.ts</files>
  <action>
    Extend `src/server-actions/solutions.ts` with final approval actions:

    1. `initiateFinalApproval(requestId: string, useCustomChain: boolean, customApproverIds?: string[])`:
       - Validate caller is in request's department
       - Validate request status is SendBackToRequester
       - Use Prisma transaction:
         a. Update request status to FinalApproval
         b. Create RequestApproval records (reuse existing model, not SolutionApproval):
            - For custom chain: create with requiredApproverId set
            - For hierarchy: use department's approval hierarchy (like Phase 3)
         c. Mark these approvals as `isFinalApproval: true` (add flag to RequestApproval model if needed)
         d. Log activity (action: 'final_approval_initiated')
         e. Notify first approver(s)
       - revalidatePath('/requests')
       - Return { success: true }

    2. `approveFinalApproval(requestId: string, comments?: string)`:
       - Reuse logic similar to existing approveRequest but:
         - Only works when status is FinalApproval
         - On last approval: change status to Completed
         - Create notification for requester (type: status_changed, message: "Request completed")
       - Could potentially reuse existing approveRequest with status check

    3. `rejectFinalApproval(requestId: string, comments: string)`:
       - Validate request status is FinalApproval
       - Validate caller can approve (current level in approval chain)
       - Use Prisma transaction:
         a. Update RequestApproval to rejected
         b. Mark remaining approvals as rejected
         c. Change request status back to SentToEngineer
         d. Log activity (action: 'final_approval_rejected')
         e. Delete the Solution? Or keep for reference? -> Keep solution, engineer can resubmit
         f. Notify engineering team
       - revalidatePath('/requests')
       - Return { success: true }

    4. `canUserApproveFinalApproval(requestId: string)`:
       - Similar to canUserApprove but only for FinalApproval status
       - Check request status is FinalApproval
       - Check user's level/specific approval matches pending approval
       - Return { canApprove, approval }

    IMPORTANT: Consider whether to reuse RequestApproval model (with new flag) or create FinalApproval model. Reusing RequestApproval with `isFinalApproval` flag is simpler.
  </action>
  <verify>
    - TypeScript compiles: `npx tsc --noEmit`
    - Functions exported: initiateFinalApproval, approveFinalApproval, rejectFinalApproval
    - Status transitions work: SendBackToRequester -> FinalApproval -> Completed
    - Rejection goes back to SentToEngineer
  </verify>
  <done>
    Final approval workflow functions handle department sign-off with custom chain support
  </done>
</task>

<task type="auto">
  <name>Task 2: Update RequestApproval model for final approval</name>
  <files>prisma/schema.prisma</files>
  <action>
    Extend RequestApproval model in `prisma/schema.prisma`:

    1. Add fields to support final approval:
       ```prisma
       model RequestApproval {
         // ... existing fields ...

         // Support for custom approval chains (like SolutionApproval)
         requiredApproverId String?
         requiredApprover   User?   @relation("RequiredApprover", fields: [requiredApproverId], references: [id])

         isCustomChain      Boolean @default(false)
         isFinalApproval    Boolean @default(false)  // Distinguish from initial department approval
       }
       ```

    2. Add relation to User model:
       ```prisma
       model User {
         // ... existing fields ...
         requiredToApprove RequestApproval[] @relation("RequiredApprover")
       }
       ```

    3. Run migration: `npx prisma db push` or `npx prisma migrate dev --name add_final_approval_fields`

    This allows RequestApproval to handle both:
    - Phase 3 department approvals (requiredLevel based)
    - Phase 4 final approvals (can be custom chain with requiredApproverId)
  </action>
  <verify>
    - `npx prisma validate` passes
    - `npx prisma db push` completes
    - RequestApproval model has isFinalApproval and requiredApproverId fields
  </verify>
  <done>
    RequestApproval model supports both hierarchy and custom chain final approvals
  </done>
</task>

<task type="auto">
  <name>Task 3: Create final approval actions component</name>
  <files>src/components/solutions/final-approval-actions.tsx</files>
  <action>
    Create `src/components/solutions/final-approval-actions.tsx`:

    1. Two components in this file:

    **InitiateFinalApprovalButton** - shown to department users when status is SendBackToRequester:
       - Props: requestId, departmentUsers (for custom picker)
       - Click opens Dialog with options:
         - Radio: "Use default department hierarchy" (default selected)
         - Radio: "Use custom approval chain"
         - If custom: show CustomApprovalPicker (reuse from 04-02)
       - Confirm button calls initiateFinalApproval
       - Loading state during submission
       - Success: toast + refresh

    **FinalApprovalActions** - shown to approvers when status is FinalApproval:
       - Props: requestId, canApprove, approval
       - Similar to SolutionApprovalActions but:
         - Approve: calls approveFinalApproval
         - Reject: calls rejectFinalApproval with required comment
         - Reject confirmation warns: "This will return the request to engineering for revision"

    2. Display logic in parent component:
       - If status === 'SendBackToRequester' && userInRequestDepartment:
         Show InitiateFinalApprovalButton
       - If status === 'FinalApproval' && canApprove:
         Show FinalApprovalActions
       - If status === 'FinalApproval' && !canApprove:
         Show "Awaiting approval from: {next approver name}"

    3. Use shadcn/ui Dialog, RadioGroup, AlertDialog, Button
  </action>
  <verify>
    - InitiateFinalApprovalButton renders and opens dialog
    - Custom approval picker integrates correctly
    - FinalApprovalActions approve/reject work
    - Rejection shows warning about returning to engineering
  </verify>
  <done>
    Final approval UI components enable department users to complete approval cycle
  </done>
</task>

<task type="auto">
  <name>Task 4: Integrate final approval into request detail modal</name>
  <files>src/components/requests/request-detail-modal.tsx</files>
  <action>
    Extend `src/components/requests/request-detail-modal.tsx`:

    1. Fetch additional data:
       - canApproveFinalApproval(requestId) when status is FinalApproval
       - Department users list for custom approval picker

    2. Add final approval section (after solution section):
       - If status === SendBackToRequester:
         - Show banner: "Solution is ready for review"
         - Show InitiateFinalApprovalButton (for department users)
         - Show solution details

       - If status === FinalApproval:
         - Show banner: "Final approval in progress"
         - Show approval progress (similar to engineering approval progress)
         - Show FinalApprovalActions if canApprove

       - If status === Completed:
         - Show banner: "Request completed"
         - Show completion date
         - All details in read-only mode

    3. Department approval progress visualization:
       - Reuse or adapt ApprovalProgress component
       - Show sequential steps with current position highlighted
       - Display who approved/rejected at each step

    4. Keep existing functionality intact
  </action>
  <verify>
    - Modal shows "Route for Final Approval" when SendBackToRequester
    - Modal shows approval progress when FinalApproval
    - Approve/reject buttons appear for eligible approvers
    - Completed status shows final read-only state
  </verify>
  <done>
    Request detail modal supports full final approval workflow
  </done>
</task>

</tasks>

<verification>
- Request in SendBackToRequester shows "Route for Final Approval" button
- Clicking opens dialog with hierarchy/custom chain options
- After initiating, status changes to FinalApproval
- Approvers see approve/reject buttons at their turn
- Rejection returns to SentToEngineer
- Final approval changes status to Completed
- All actions logged in activity trail
</verification>

<success_criteria>
1. Department users can initiate final approval from SendBackToRequester
2. Custom approval chain works for final approval
3. Default department hierarchy works for final approval
4. Rejection returns request to SentToEngineer for engineering revision
5. Final approval completion marks request as Completed
6. Email notifications sent to final approvers
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-05-SUMMARY.md`
</output>
