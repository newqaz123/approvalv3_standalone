---
phase: 04-engineering-solutions
plan: 17
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/engineering/page.tsx
  - src/components/engineering/needs-action-list.tsx
autonomous: false
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "Clicking 'All Engineering Requests' tab displays list without 404 error"
    - "Clicking 'Review & Approve' button opens request detail modal"
    - "Modal shows request details with approval buttons"
    - "No full page navigation occurs when clicking 'Review & Approve'"
  artifacts:
    - path: "src/app/(dashboard)/engineering/page.tsx"
      provides: "Engineering dashboard with client-side tab filtering"
      lines: [138-154]
    - path: "src/components/engineering/needs-action-list.tsx"
      provides: "Review & Approve button with modal state management"
      lines: [208-212]
    - path: "src/components/requests/request-table.tsx"
      provides: "Reference pattern for modal state management"
      lines: [40-48, 162-168]
  key_links:
    - from: "src/components/engineering/needs-action-list.tsx"
      to: "src/components/requests/request-detail-modal.tsx"
      via: "useState pattern with selectedRequestId and isModalOpen"
      pattern: "useState.*selectedRequestId"
    - from: "src/app/(dashboard)/engineering/page.tsx"
      to: "client-side tab state"
      via: "URL param ?tab=all instead of route navigation"
      pattern: "\\?tab="
---

<objective>
Fix engineering dashboard navigation issues: 404 error on "All Engineering Requests" tab and incorrect redirect when clicking "Review & Approve" button.

Purpose: Two distinct bugs: (1) Tab navigation links to non-existent route causing 404, (2) "Review & Approve" button uses window.location.href causing full page navigation instead of opening modal. This plan implements client-side tab filtering and proper modal state management.

Output: Working dashboard tabs with no 404 errors and modal opens correctly for "Review & Approve" action.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-engineering-solutions/04-engineering-solutions-UAT.md
@.planning/debug/engineering-dashboard-404-redirect.md
@src/app/(dashboard)/engineering/page.tsx
@src/components/engineering/needs-action-list.tsx
@src/components/requests/request-table.tsx
@src/components/requests/request-detail-modal.tsx
</context>

<tasks>

<task type="auto">
  <name>Fix 'All Engineering Requests' tab to use client-side filtering</name>
  <files>src/app/(dashboard)/engineering/page.tsx</files>
  <action>
    Replace the broken tab navigation at line 148 with client-side tab filtering:

    Current code:
    ```typescript
    <a href="/engineering/requests">
    ```

    Change to client-side tab state:
    ```typescript
    <button
      onClick={() => setActiveTab('all')}
      className={activeTab === 'all' ? '...' : '...'}
    >
      All Engineering Requests
    </button>
    ```

    Then update the data fetching/display logic to show all engineering requests when activeTab === 'all':
    - If showing 'needs-action': Display needsSolution and needsApproval lists
    - If showing 'all': Display all requests where department is engineering or requests in engineering workflow

    Why: Link to /engineering/requests causes 404 because route doesn't exist. Client-side filtering is simpler than creating new route and matches existing tab pattern.

    Alternative (if you prefer URL params):
    ```typescript
    <Link href="/engineering?tab=all" replace>
    ```
    Then read tab from searchParams: `const tab = searchParams?.tab || 'needs-action'`

    Reference: .planning/debug/engineering-dashboard-404-redirect.md
  </action>
  <verify>grep -n "href=\"/engineering/requests\"" src/app/(dashboard)/engineering/page.tsx | wc -l | grep -q "^0$" && grep -n "activeTab" src/app/(dashboard)/engineering/page.tsx | grep -q "setActiveTab"</verify>
  <done>Tab navigation uses client-side state (setActiveTab) or URL params instead of broken route link, eliminating 404 error</done>
</task>

<task type="auto">
  <name>Fix 'Review & Approve' button to open modal instead of redirecting</name>
  <files>src/components/engineering/needs-action-list.tsx</files>
  <action>
    Replace the window.location.href navigation with proper modal state management:

    1. Import RequestDetailModal at top:
    ```typescript
    import { RequestDetailModal } from '@/components/requests/request-detail-modal'
    ```

    2. Add state variables after existing state (around line 50):
    ```typescript
    const [selectedRequestId, setSelectedRequestId] = useState<string | null>(null)
    const [isModalOpen, setIsModalOpen] = useState(false)
    ```

    3. Replace the onClick handler at line 211:
    ```typescript
    // OLD: window.location.href = `/requests?open=${request.id}`
    // NEW:
    onClick={() => {
      setSelectedRequestId(request.id)
      setIsModalOpen(true)
    }}
    ```

    4. Add RequestDetailModal component at bottom before closing </div>:
    ```typescript
    {selectedRequestId && (
      <RequestDetailModal
        requestId={selectedRequestId}
        open={isModalOpen}
        onOpenChange={setIsModalOpen}
      />
    )}
    ```

    Why: window.location.href causes full page navigation to /requests?page instead of opening modal. Follow pattern from request-table.tsx (lines 40-48, 162-168) which correctly uses state management.

    Reference: .planning/debug/engineering-dashboard-404-redirect.md
  </action>
  <verify>grep -n "window.location.href" src/components/engineering/needs-action-list.tsx | grep -v "^//" | wc -l | grep -q "^0$" && grep -n "RequestDetailModal" src/components/engineering/needs-action-list.tsx | grep -q "import"</verify>
  <done>Button click opens modal using useState pattern (selectedRequestId, isModalOpen) instead of full page navigation, modal displays correctly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fixed two dashboard navigation bugs:
    1. "All Engineering Requests" tab now uses client-side filtering instead of broken route link
    2. "Review & Approve" button now opens modal using useState pattern instead of redirecting
  </what-built>
  <how-to-verify>
    Test 1: "All Engineering Requests" tab
    - Navigate to /engineering
    - Click "All Engineering Requests" tab
    - Expected: Tab switches without page reload, shows list of engineering requests
    - Expected: NO 404 error

    Test 2: "Review & Approve" button from "Requests Awaiting Solution"
    - Navigate to /engineering
    - Find "Requests Awaiting Solution" section
    - Click "Review & Approve" button on any request
    - Expected: Modal opens with request details
    - Expected: NO full page navigation
    - Expected: URL remains /engineering (not /requests)

    Test 3: "Review & Approve" button from "Solutions Awaiting Approval"
    - Navigate to /engineering
    - Find "Solutions Awaiting Your Approval" section
    - Click "Review & Approve" button
    - Expected: Modal opens with solution details and approval buttons
    - Expected: Modal closes when clicking X or outside

    Test 4: Modal functionality
    - With modal open, try approving/rejecting
    - Expected: All modal actions work correctly
    - Expected: Modal closes after action completes
  </how-to-verify>
  <resume-signal>Type "approved" if navigation works correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Clicking "All Engineering Requests" tab switches view without 404 error
2. Tab navigation uses client-side state or URL params (not broken route)
3. "Review & Approve" button opens modal without full page navigation
4. Modal displays request/solution details correctly
5. Modal actions (approve/reject/close) work as expected
</verification>

<success_criteria>
- Dashboard tabs work correctly with no 404 errors
- "Review & Approve" opens modal in context (doesn't navigate away)
- User experience matches pattern from main request list
- Modal state properly managed (open/close/select)
</success_criteria>

<output>
After completion, create `.planning/phases/04-engineering-solutions/04-17-SUMMARY.md`
</output>
